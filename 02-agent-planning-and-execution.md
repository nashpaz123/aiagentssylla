<div dir="rtl" lang="he">

<p align="center">
  <img src="assets/sprint-tech-academy-logo.png" alt="SPRINT Tech Academy" width="420" />
</p>

# ‏מפגש 2 — Agent Architecture: Planning ו-Execution
## ‏קובץ מרצה (תסריט מלא להקלטה)

> **משך מיועד:** ~2.5–3 שעות **הקלטה רצופה**  
> **קהל בהקלטה:** אין זמן עבודה לסטודנטים חיים · אין שקט לחשיבה · אין המתנה לתשובות מקהל חי  
> **תרגילים/הדגמות:** המרצה מקריא פתרון מלא · אין זמן פתרון עצמאי של צופים  
> **מצגת סטודנטים:** [`02-students-slides.md`](02-students-slides.md)  
> **Lab:** [`labs/02-aws-agent-loop/`](labs/02-aws-agent-loop/) · פרופיל `nashpazformatan` · `eu-north-1`

> מספור המרצה = מספור הסטודנטים (1…46). לא מפרטים שוב את מפגש 1.

---

# ‏הנחיות למרצה (הקלטה)

- מקריאים את בלוק **להקראה** במלואו; מותר להרחיב אבל לא לקצר את הפירוט על הדוגמאות.
- **`[סטודנטים · N]`** — גללו למספר N לפני ההקראה.
- **אין** סימוני המתנה לקהל. שאלות רטוריות נענות מיד בקול המרצה.
- **`[Lab חי]`** — מסוף / Claude Code לפי `labs/02-aws-agent-loop/`.
- כל בלוק קוד אצל המרצה מופיע גם אצל הסטודנטים באותו מספר.

---

# ‏חלק א׳ — פתיחה

## ‏1 — כותרת

[סטודנטים · 1]

**על המסך:**

<p align="center">
  <img src="assets/sprint-tech-academy-logo.png" alt="SPRINT Tech Academy" width="360" />
</p>

**Agent Architecture**  
Planning ו-Execution

![Goal → Plan → Actions](assets/02/01-goal-plan-actions.png)

**להקראה:**

ברוכים הבאים למפגש השני בקורס AI Agents ומערכות אוטונומיות.

על המסך הכותרת: Agent Architecture — Planning ו-Execution, עם הלוגו של SPRINT Tech Academy והדיאגרמה Goal → Plan → Actions.

במפגש הקודם בנינו שפה משותפת. היום אנחנו לא חוזרים על זה בפירוט. היום יורדים לתוך המנוע עצמו.

המטרה של המפגש היא שתצאו עם הבנה מבצעית: איך Agent בונה תוכנית, איך הוא מבצע אותה מול כלים, איך הוא מגיב לכישלון, מתי הוא מתכנן מחדש, ואיך שמים גבולות שלא יהפכו את המערכת ללולאה יקרה.

בהמשך נריץ גם Lab חי קטן מול AWS — קריאה בלבד — ונראה Re-plan על Observation אמיתי. וגם נעבור הדגמות פתרון מלאות של תרחישי CI ו-Design Review.

הדיאגרמה על המסך היא מפת הדרכים של היום. כל פעם שתשמעו Goal, Plan, Actions, Result — תחזירו את הראש לשרשרת הזו. אם משהו לא יושב על אחד מארבעת התיבות — כנראה שהוא עדיין לא ממוסגר נכון בארכיטקטורה.

אם תראו על המסך דיאגרמה, אני לא אגיד רק דיאגרמה יפה. אני אעבור על החיצים. Goal נכנס, Plan מחליט, Actions נוגעים בעולם, Result חוזר פנימה. זה המעגל שנפרק היום לחלקים.

---

## ‏2 — מה היום (+ חזרה קצרה על מפגש 1)

[סטודנטים · 2]

**על המסך:**

| נבנה היום | מפגש 1 (בקצרה) |
|---|---|
| Planning / Decomposition | מהו Agent מול Chat |
| Open/Closed · ReAct · Plan-and-Execute | Spectrum L0–L6 |
| Validation · Failure · Budget · State | HITL ברמת מבוא |
| Lab חי: Claude Code + AWS (read-only) | |

**להקראה:**

שימו לב לטבלה על המסך.

בטור השמאלי — מה שבונים היום: Planning ו-Decomposition, Open מול Closed Loop, ReAct, Plan-and-Execute, Validation, Failure Handling, Budget ו-State, ו-Lab חי עם Claude Code ו-AWS.

בטור הימני — מה שלא נפרט שוב: ההגדרה הבסיסית של Agent מול Chat, הספקטרום L0 עד L6, ו-HITL ברמת מבוא. אם משהו משם חסר לכם — חזרו למפגש 1. במפגש הזה אנחנו מתקדמים קדימה כדי להספיק עומק.

הסיבה לזה חשובה: אם נחזור על מפגש 1, נישאר שעה על מושגים שכבר כיסינו ולא נגיע לביצוע האמיתי. ולכן מהיום ואילך — פחות חזרה, יותר פירוט על דוגמאות ועל לולאות עבודה.

חשוב לי להדגיש שוב את המשמעת הזו להמשך הקורס, לא רק למפגש 2. ממפגש 2 והלאה אנחנו מניחים את הבסיס של מפגש 1 כנתון. אם נחזור עליו בכל מפגש — לא נגיע לעומק של Tools, Graphs, Multi-Agent, Memory ו-Production.

ולכן אם אתם מרגישים שחסר לכם מושג בסיסי — זה סימן לחזור למפגש 1 בנפרד, ואז לחזור לכאן.

במילים פשוטות: מפגש 1 לימד מה זה. מפגש 2 מלמד איך זה רץ בלי להתפרק. כל שאר המפגשים יניחו את איך זה רץ כבר קיים, ולכן חייבים לצאת מכאן עם שלד חזק.

עוד מילה על מה שלא נפרט: Spectrum ו-HITL בסיסי כבר קיבלו במה במפגש 1. היום HITL יופיע כחלק מ-Policy Gate ו-Design Review — כלומר בהקשר של ביצוע, לא כהגדרה מחדש.

---

## ‏3 — שאלת המפגש + תוצרים

[סטודנטים · 3]

**על המסך:**

> מטרה גדולה → איך בוחרים צעד ראשון, ומתי משנים תוכנית?

בסוף המפגש תדעו:
1. לפרק מטרה ל-Hypothesis Plan  
2. להבדיל Tool / Step / Goal success  
3. לתכנן Failure + Stop + Budget  
4. להריץ לולאה חיה ולזהות Re-plan

**להקראה:**

השאלה שתלווה את כל המפגש כתובה על המסך:

אם יש מטרה גדולה — איך בוחרים את הצעד הראשון, ומתי משנים את התוכנית.

זו לא שאלה פילוסופית. זו שאלת ארכיטקטורה. כי ברגע שיש לכם LLM עם כלים, קל מאוד לתת לו מטרה ולקוות לטוב. בפועל בלי Planning מפורש, בלי Validation, ובלי Stop — מקבלים מערכת יקרה, איטית, ולפעמים מסוכנת.

ארבעת התוצרים בסוף המפגש:

ראשית — לפרק מטרה ל-Hypothesis Plan, כלומר תוכנית שהיא השערה ולא חוזה.

שנית — להבדיל בין שלושה סוגי הצלחה: Tool, Step, ו-Goal.

שלישית — לתכנן Failure Handling, Stop Conditions, ו-Budget.

ורביעית — להריץ לולאה חיה ולזהות רגע של Re-plan.

אם אתם זוכרים רק את ארבע הנקודות האלה בסוף המפגש — המפגש הצליח.

בואו נפרק את ארבעת התוצרים לשפת בדיקה עצמית בסוף המפגש.

אם מישהו נותן לכם מטרה עמומה — האם אתם יודעים להפוך אותה לרשימת צעדים עם תלויות?

אם Tool מחזיר מאתיים — האם אתם יודעים לשאול האם המשימה באמת התקדמה?

אם Agent נתקע — האם יש לכם Budget ו-Stop כתובים, או רק תקווה?

ואם ראיתם Observation מפתיע — האם אתם יודעים להבדיל בין Local re-plan ל-Global?

ארבע התשובות האלה הן עמוד השדרה של שאר המפגש.

שימו לב שהשאלה על המסך לא שואלת איזה מודל הכי טוב. היא שואלת איך בוחרים צעד ומתי משנים תוכנית. זו שאלת מערכת. לאורך כל המפגש אחזיר אתכם לשאלה הזו אחרי כל דוגמה.

תוצר ראשון בפירוט: Hypothesis Plan אומר שאפשר לכתוב את הצעדים, וגם לכתוב מה יגרום לשנות אותם. תוצר שני: שלושת סוגי ההצלחה הם שפה משותפת לדיבאג. תוצר שלישי: Budget ו-Stop הם חלקי מוצר. תוצר רביעי: Lab מוכיח שזה לא תאוריה.

השאלה של המפגש תחזור גם ב-Lab וגם בהדגמות הפתרון. כל פעם נענה עליה שוב עם עוד שכבת פירוט.

---

# ‏חלק ב׳ — Planning לעומק

## ‏4 — Planning = גשר

[סטודנטים · 4]

**על המסך:**

**הגדרה:** Planning — דרך אפשרית ממטרה לתוצאה (השערה, לא חוזה).

```text
GOAL → PLAN → ACTIONS → RESULT
```

דוגמה אנושית (קצר):
מטרה: *״חופשה ברומא באוקטובר עד ₪6,000.״*  
תוכנית: תאריכים → טיסות → מלון → אטרקציות → Buffer לביטולים.

**להקראה:**

Planning הוא גשר.

על המסך הזרימה: GOAL עוברת ל-PLAN, משם ל-ACTIONS, ומשם ל-RESULT.

מטרה אומרת לאן רוצים להגיע. תוכנית אומרת איך מנסים להגיע לשם עכשיו. פעולות הן מה שקורה מול כלים ומערכות. תוצאה היא מה שבאמת יצא.

ניקח את הדוגמה האנושית על המסך ונפרק אותה לאט.

המטרה: חופשה ברומא באוקטובר עד שישה אלפים שקל. זו מטרה. היא לא אומרת איזה אתר לפתוח קודם, ולא אומרת מה קורה אם הטיסה יקרה מדי.

התוכנית האנושית הטיפוסית: קודם סוגרים תאריכים גמישים. אחר כך בודקים טיסות. אחר כך מלון באזור סביר. אחר כך אטרקציות עיקריות. ובסוף משאירים Buffer לביטולים וחריגות.

שימו לב מה קורה אם אחרי בדיקת הטיסות מתברר שהמחיר חורג מהתקציב. האדם לא ממשיך בעיוורון למלון היקר שתכנן. הוא משנה תוכנית: יעד אחר, תאריכים אחרים, או מלון זול יותר. זו בדיוק המשמעות של Planning כהשערה.

אצל Agent אותו רעיון: המטרה מגיעה מהמשתמש או מהמערכת. התוכנית נבנית לפי הקשר ואילוצים. הפעולות הן Tool Calls. והתוצאה נמדדת מול Success Criteria — לא מול תחושת בטן של המודל.

עוד שכבה על דוגמת החופשה, כי חשוב לראות את ה-Re-plan האנושי בבירור.

נניח שאחרי בדיקת טיסות גיליתם שהמחיר לרומא חורג באלפיים שקל. אדם סביר לא ממשיך להזמין מלון באותו תקציב כאילו כלום לא קרה. הוא מעדכן את התוכנית: אולי משנה תאריכים באמצע שבוע, אולי יעד חלופי, אולי מלון מחוץ למרכז.

עכשיו תרגמו ל-Agent: Observation חדש על עלות Tool או על תוצאת API חייב להיות מסוגל לשנות את התוכנית. אם המערכת ממשיכה בעיוורון — אין לכם Agent. יש לכם Script עם LLM באמצע.

נחדד עוד הבדל: Result הוא לא אותו דבר כמו Output של המודל. Output יכול להיות טקסט יפה. Result נמדד מול העולם — קובץ שנוצר, תקלה שנמצאה, או פעולה שבוצעה. Planning בלי מדידת Result הוא מצגת, לא מערכת.

נחזור לשרשרת עם דוגמת DevOps במקביל לדוגמת החופשה. מטרה: הבן למה ה-API איטי. תוכנית: מדדים, אחר כך לוגי זמן, אחר כך deploy, אחר כך DB. פעולה: קריאות כלים. תוצאה: דוח עם סיבה סבירה או Escalate.

אם אחרי מדדים מתברר שאין בכלל עלייה — Result הוא False alarm, והתוכנית נעצרת. זה Planning מוצלח, לא כישלון. כי המטרה הייתה להבין, לא בהכרח למצוא באג שלא קיים.

Planning הוא גם תיעוד. תוכנית שכתובה מאפשרת Review. תוכנית שחייה רק בתוך המודל לא מאפשרת ביקורת אמיתית לפני פעולה.

---

## ‏5 — משימה פשוטה מול מורכבת

[סטודנטים · 5]

**על המסך:**

| פשוטה | מורכבת |
|---|---|
| Ping לשרת | חקירת אי־יציבות מאז Release |
| כמעט בלי Planning | סדרת החלטות + Evidence |

תרחישי עבודה:
1. *״העתק קובץ A→B״* — Workflow  
2. *״למה הלקוח כועס על חיוב?״* — Agent  
3. *״Deploy ל־prod אחרי בדיקות״* — היברידי (Plan + Policy)

**להקראה:**

לא כל משימה צריכה Planning עמוק.

בטבלה: משימה פשוטה מול מורכבת.

פשוטה: Ping לשרת. כמעט אין אי־ודאות. יש כלי, יש בדיקה, יש תשובה. אפשר Workflow.

מורכבת: חקירת אי־יציבות מאז Release. כאן יש כמה השערות אפשריות, כמה מקורות מידע, וסדר הפעולות תלוי במה שמתגלה.

עכשיו שלושת תרחישי העבודה על המסך — נפרט כל אחד.

תרחיש אחד: העתק קובץ מ-A ל-B. זה תהליך דטרמיניסטי. אם נכשל — retry או התראה. אין כאן Agent אמיתי. זה Workflow, אולי עם AI קטן בניסוח השגיאה.

תרחיש שניים: למה הלקוח כועס על חיוב. כאן צריך לקרוא מייל, לחפש ב-CRM, לבדוק חשבוניות, אולי לחפש בלוגים של תשלומים. הסדר לא תמיד ידוע מראש. זו משימת Agent קלאסית.

תרחיש שלוש: Deploy ל-Production אחרי בדיקות. חלק מהתהליך חייב להיות קשיח: סריקות, אישורים, סביבות. חלק אחר יכול להיות גמיש: איך מנתחים כשל בטסט. זה היברידי — Plan עם Policy, ו-Agent רק בתוך גבולות מותרים.

המסר: Planning שימושי כשיש אי־ודאות וצריך לבחור מסלול. בלי אי־ודאות — אל תסבכו.

עוד דוגמה לכל טור, כדי לקבע.

בשורה הפשוטה: בדיקת DNS או Healthcheck. כאן Planning עמוק הוא over-engineering.

בשורה המורכבת: לקוח מדווח על חיוב כפול אחרי שדרוג תוכנית. צריך CRM, Billing, Logs, אולי Fraud. הסדר תלוי במה שמופיע קודם. זה Agent.

ובהיברידי: Pipeline של Release. השלבים הקשיחים נשארים בקוד. Agent יכול לעזור לנתח כשל טסט או להציע השערות — אבל לא לדלג על Approval.

אם אתם מתלבטים בין שלוש השורות בטבלה עבור משימה מהעבודה שלכם, שאלו: כמה ענפים אפשריים יש לפני הצעד הראשון. אם רק אחד — Workflow. אם כמה והבחירה תלויה במידע חדש — Agent. אם יש מסלול קשיח עם כיס גמיש — היברידי.

עוד דוגמה פשוטה: לנקות קבצי temp כל לילה. אין כאן Agent. יש Cron. אם תשימו LLM באמצע בלי סיבה — הוספתם נקודת כשל.

עוד דוגמה מורכבת: לקוח ממספר מדינות מדווח על מס שגוי בחשבונית. צריך כללי מס, CRM, Billing, ולפעמים מסמך חוקי. הסדר משתנה לפי המדינה שזוהתה. זה Agent עם Constraints כבדים.

זיהוי מהיר: אם אפשר לכתוב if/then מלא מראש — אל תביאו Agent. אם if/then מתפוצץ מענפים — כן.

---

## ‏6 — תוכנית = Hypothesis + רמות פירוט

[סטודנטים · 6]

**על המסך:**

```text
1. Get logs
2. Check metrics
3. Check deployments
4. Analyze
```

או כוונה מופשטת: `Investigate recent instability` + החלטות מקומיות.

רמות: **Full plan מראש** · **Skeleton** · **Step-wise (ReAct)**.

**להקראה:**

תוכנית היא Hypothesis — השערת עבודה.

על המסך רשימה מפורטת לדוגמה: Get logs, Check metrics, Check deployments, Analyze.

זו רמה אחת של Planning: Full plan מראש. נוח לביקורת, נוח ל-HITL, נוח ל-Debug. החיסרון: עלולה להיות שגויה כבר מהצעד השני.

רמה שנייה: Skeleton. למשל Investigate recent instability. מתחת לזה Agent מחליט מקומית אילו כלים לפתוח. גמיש יותר, פחות ניתן לביקורת מראש.

רמה שלישית: Step-wise כמו ReAct. כל צעד נולד מהתצפית הקודמת. מקסימום גמישות, מקסימום סיכון להתפזרות.

חשוב להגיד בקול: אין רמה אחת נכונה תמיד. יש התאמה להקשר. חקירת תקלה לא מוכרת — נוטים ל-Skeleton או ReAct עם Budget. שינוי Production — נוטים ל-Full plan עם Policy Gate לפני ביצוע.

ואחרי Observation ראשון — בכל הרמות — מותר ואף רצוי לשנות תוכנית. Hypothesis שלא משתנה מול עובדות חדשות היא לא משמעת — היא עקשנות.

איך בוחרים רמת פירוט בפועל?

אם יש HITL לפני פעולה מסוכנת — עדיף Full plan קריא לאדם.

אם החקירה פתוחה לגמרי ואין פעולת כתיבה — Skeleton או ReAct עם Budget יכולים להיות יעילים יותר.

אם אתם לא בטוחים — התחילו מ-Skeleton קצר של שלושה עד חמישה צעדים, לא מארבעים. תמיד אפשר לפרק עוד אחרי Observation ראשון. הרבה יותר קשה לסלוח על Overplanning ששורף תקציב לפני שהבנתם את הבעיה.

דוגמה לרמת Skeleton טובה: אחת, אסוף סימפטומים. שתיים, בנה Hypothesis. שלוש, אסוף Evidence. ארבע, החלט Stop או Escalate. מתחת לכל סעיף Agent בוחר Tools. זו מסגרת בלי Overplanning.

Full plan מתאים כשאתם יודעים פחות או יותר את תחנות החקירה. Skeleton מתאים כשאתם יודעים רק את השלבים הלוגיים. ReAct מתאים כשאתם יודעים בעיקר את הכלים ואת המטרה.

טעות נפוצה: לבחור ReAct כי זה נשמע מתקדם, ואז לגלות בפרודקשן שאין Trace ואין Budget. הבחירה צריכה להתחיל מהסיכון לא מהטרנד.

Hypothesis חייבת להיות ניתנת להפרכה. אם אין Observation שיכול להפיל אותה — זו דוגמה, לא Hypothesis.

---

## ‏7 — סוגי טעויות Planning

[סטודנטים · 7]

**על המסך:**

| סוג | דוגמה |
|---|---|
| Missing step | Deploy בלי Security Scan |
| Wrong order | Rollback לפני אבחון |
| Wrong assumption | ״תמיד Runner אשם״ |
| Overplanning | 40 צעדים למשימה של 5 |
| Underplanning | ״תתקן את הפרוד״ כצעד אחד |

**להקראה:**

עכשיו ארגז כלים לביקורת תוכניות: חמישה סוגי טעויות Planning.

Missing step: חסר שלב קריטי. דוגמה קלאסית — Deploy בלי Security Scan. התוכנית נראית קצרה ויפה, ובדיוק בגלל זה היא מסוכנת.

Wrong order: השלבים קיימים אבל בסדר הפוך. Rollback לפני אבחון עלול להסתיר את הבעיה או ליצור נזק כפול.

Wrong assumption: התוכנית מניחה עובדה שגויה. למשל תמיד Runner אשם ב-CI. זו מלכודת נפוצה אצל Agents — הם מתאהבים בהשערה הראשונה.

Overplanning: ארבעים צעדים למשימה שצריכה חמישה. יותר State, יותר latency, יותר נקודות כשל, יותר Tokens.

Underplanning: צעד אחד בשם תתקן את הפרודקשן. זה לא Plan — זו משאלה.

כשאתם בונים או סוקרים Agent, עברו על התוכנית עם חמש המשקפיים האלה. הרבה כשלים ב-Production מתחילים כאן — עוד לפני בחירת המודל.

ניקח דוגמת CI קצרה לכל סוג טעות.

Missing step: מסיקים מה נשבר בלי לקרוא לוג.

Wrong order: מציעים שינוי ל-requirements לפני שמוצאים את שורת ה-ImportError.

Wrong assumption: Runner אשם כי ככה היה בשבוע שעבר.

Overplanning: עשרים בדיקות סביבתיות לפני סטטוס ה-Job.

Underplanning: צעד אחד בשם תתקן את הבילד.

אם תתרגלו לסווג כשלים כך — תתפסו בעיות תכנון לפני שהן הופכות לאירוע Production.

Wrong assumption הוא מסוכן במיוחד כי הוא נראה כמו ביטחון. המודל מדבר חלק, Confidence גבוהה, והצוות נרדם. לכן ב-State כדאי לשמור גם rejected_hypotheses — רשימה של מה כבר הופרך. זה מונע לחזור לאותה טעות בסיבוב הבא.

Overplanning ו-Underplanning הם שני קצוות של אותה בעיה: חוסר התאמה בין פירוט לאי־ודאות. כשיש אי־ודאות גבוהה — אל תנעלו ארבעים צעדים. כשיש אי־ודאות נמוכה — אל תעזבו צעד אחד עמום.

Wrong order בטיפוסי Support: שולחים פיצוי לפני שווידאו שיש חיוב כפול. זה לא רק באג תהליך — זה כסף.

Missing step שכיח ב-Agents: אין צעד של בדיקת הרשאות לפני קריאה ראשונה. ואז כל החקירה מתחילה ב-Permission denied מיותר.

---

# ‏חלק ג׳ — Task Decomposition

## ‏8 — Task Decomposition

[סטודנטים · 8]

**על המסך:**

![Task Decomposition](assets/02/02-task-decomposition.png)

**הגדרה:** פירוק לתת־משימות שניתן לבצע, לבדוק ולהיכשל בהן בבירור.

כלל: יחידת משמעות — לא מספר יפה של שורות.

**להקראה:**

Task Decomposition.

על המסך הדיאגרמה וההגדרה: פירוק משימה גדולה לתת־משימות שאפשר לבצע, לבדוק, ולהיכשל בהן בצורה ברורה.

למה זה חשוב? כי מטרה גדולה היא בדרך כלל עמומה מדי לפעולה ישירה. שפר את ביצועי השירות — מה זה אומר? CPU? Database? Network? Cache? קוד? בלי פירוק, Agent מהמר.

כלל האצבע: מפרקים לפי יחידת משמעות — לא לפי מספר שורות יפה במצגת.

כל Step צריך לעמוד בארבעה מבחנים. האם מבינים אותו? האם אפשר לבצע אותו עם Tool קיים? האם אפשר לבדוק שהצליח? והאם כישלון שלו ברור מספיק כדי להחליט מה הלאה?

פירוק דק מדי יוצר רעש: עשרות צעדים זעירים, State כבד, ו-latency גבוה. פירוק גס מדי מחזיר אתכם למטרה עמומה. המטרה היא אמצע מדויק מספיק לעבודה.

עוד קריטריון מעשי לפירוק: האם אפשר לתת שם ל-Tool Call ברור לכל Step.

אם אתם לא יודעים איזה Tool משרת את הצעד — הצעד עדיין עמום. או שחסר Tool, או שהצעד גדול מדי.

ולהפך: אם צעד אחד דורש חמישה Tools בלתי קשורים — כנראה צריך לפרק אותו. Decomposition טוב מתיישב עם גבולות הכלים שיש לכם באמת.

מבחן מהיר לפירוק: האם אפשר לחלק את העבודה בין שני אנשים בלי שידרסו אחד את השני. אם כן — כנראה יש תת־משימות אמיתיות. אם לא — כנראה עדיין יש גוש אחד גדול מדי.

כשאתם מפרקים, כתבו ליד כל Step את ה-Observation הצפוי. למשל אחרי get_metrics מצפים לערך p95 וזמן התחלה. אם אין צפי — הצעד עדיין לא חד.

זה מאלץ Validation כבר בשלב התכנון, לא רק אחרי הקוד.

Decomposition טוב מוריד גם עלות אנושית: קל יותר לחלק בעלות בין תורנים כשיש צעדים ברורים.

---

## ‏9 — Decomposition בדוגמת Latency

[סטודנטים · 9]

**על המסך:**

מטרה: *״מצא למה latency עלה.״*

```text
1. Check when latency increased
2. Compare with deployments
3. Check CPU / Memory
4. Check database latency
5. Inspect application logs
6. Correlate findings
7. Identify most likely cause
```

Hypothesis ראשונית — לא חוזה.

**להקראה:**

דוגמת Latency — נעבור שורה־שורה.

המטרה: מצא למה latency עלה.

צעד אחד: Check when latency increased. בלי זה אין ציר זמן, ואין Correlation אפשרי.

צעד שניים: Compare with deployments. מחפשים שינוי שקרוב בזמן. חשוב: קרבה בזמן היא רמז — לא הוכחה.

צעד שלוש: Check CPU ו-Memory. האם זה לחץ משאבים פשוט.

צעד ארבע: Check database latency. הרבה עליות latency ב-API נולדות ב-DB.

צעד חמש: Inspect application logs. מחפשים timeouts, errors, stack traces.

צעד שש: Correlate findings. מחברים ציר זמן, deploy, מדדים ולוגים.

צעד שבע: Identify most likely cause. לא כל הסיבות האפשריות — הסיבה הסבירה ביותר עם Evidence.

עכשיו נקודה קריטית: אחרי הצעד הראשון יכול להתברר שהעלייה הייתה spike קצר שנעלם. במקרה כזה אין טעם להמשיך את כל הרשימה. זו המשמעות של Hypothesis — הרשימה הזאת היא נקודת התחלה, לא חוזה עם המציאות.

סימולציה קצרה בקול: מה אם אחרי צעד שני אין Deploy קרוב בזמן?

אז Correlation עם Release נחלש. Local re-plan יכול להקפיץ קדימה בדיקת DB או תלות חיצונית, במקום לבזבז זמן על היסטוריית גרסאות לא רלוונטית.

מה אם הלוגים מלאים ב-timeout ל-DB? אז Confidence לכיוון DB עולה, ואפשר לצמצם את שאר הענפים. שוב — Hypothesis חיה, לא רשימת מכולת.

עוד ענף: מה אם CPU גבוה וגם DB latency גבוה. אז Correlation כפול. לא בוחרים סיבה אחת מתוך נוחות. אוספים עוד Evidence או מדווחים על שתי השערות מתחרות עם Confidence לכל אחת. יושרה עדיפה על סיפור חלק.

נריץ בקול תרחיש חלופי מלא. צעד 1 מראה עלייה. צעד 2 מראה אין deploy. צעד 3 CPU נורמלי. צעד 4 DB latency גבוה. צעד 5 לוגים עם lock wait. צעד 6 Correlate לשינוי אינדקס. צעד 7 סיבה סבירה: נעילת DB אחרי שינוי סכימה. שימו לב שהרשימה המקורית עדיין שירתה אותנו — אבל הדגשים השתנו אחרי Observation.

בסוף דוגמת ה-latency אמרו בקול: זו תבנית שאפשר להעתיק ל-runbook של Agent, לא רק לסיפור חד־פעמי.

---

## ‏10 — דוגמה מפורטת: Decomposition לחיסכון בענן

[סטודנטים · 10]

**על המסך:**

מטרה: *״הורד את חשבון הענן החודשי ב־20% בלי לפגוע ב־SLA.״*

פירוק שמדגימים בקול:
```text
1. Baseline: עלות 30 יום לפי שירות
2. מצא Top-5 עלויות
3. בדוק idle / over-provision (non-prod קודם)
4. הצע rightsizing עם הערכת חיסכון
5. בדוק השפעה על latency/error budget
6. Policy: אין שינוי prod בלי אישור
7. דוח: חיסכון צפוי + סיכונים + צעדי HITL
```

תלויות: 1→2→3; 4 תלוי ב־3; 5 לפני כל שינוי; 6 חוסם ביצוע אוטומטי.

**להקראה:**

עכשיו דוגמה מפורטת נוספת — חיסכון בענן — ונעבור עליה כהדגמה מלאה.

המטרה: הורד את חשבון הענן החודשי בעשרים אחוז בלי לפגוע ב-SLA.

צעד אחד: Baseline — עלות שלושים יום לפי שירות. בלי baseline אין איך לדעת אם חסכתם.

צעד שניים: מצא Top-5 עלויות. מתמקדים איפה הכסף באמת.

צעד שלוש: בדוק idle ו-over-provision, קודם ב-non-prod. שם הסיכון נמוך והחיסכון לעיתים גדול.

צעד ארבע: הצע rightsizing עם הערכת חיסכון מספרית. לא רק תקטין — אלא כמה בערך ייחסך.

צעד חמש: בדוק השפעה על latency ו-error budget. בלי זה החיסכון עלול לשבור SLA.

צעד שש: Policy — אין שינוי Production בלי אישור. זה לא סעיף יפה — זה Gate.

צעד שבע: דוח — חיסכון צפוי, סיכונים, וצעדים שדורשים HITL.

תלויות: אחד לפני שניים לפני שלוש. ארבע תלוי בשלוש. חמש לפני כל שינוי אמיתי. שש חוסם ביצוע אוטומטי לפרוד. אפשר לעשות חלקים ממחקר העלויות במקביל רק אחרי שיש baseline.

שימו לב במיוחד לסעיף שישה בדוגמת הענן: Policy לפני שינוי Production.

הרבה צוותים מדלגים על זה כי החיסכון מפתה. ואז Agent מציע rightsizing לפרודקשן בלי אישור, ומישהו לוחץ Approve מתוך עייפות.

בארכיטקטורה נכונה, סעיף ה-Policy הוא לא המלצה בסוף הדוח. הוא Gate שחוסם Tool של שינוי עד שיש אישור. את זה חייבים לקבע כבר ב-Decomposition — לא כהערת שוליים אחרי תקרית.

בדוגמת הענן, שימו לב ש-non-prod קודם. זה לא רק בטיחות — זה גם סטטיסטיקה. הרבה בזבוז יושב בסביבות פיתוח ובדיקות. Agent שרץ ישר לפרודקשן מחפש דרמה במקום חיסכון קל.

בדוגמת הענן נוסיף מספרים מדומים כדי להרגיש מציאות. Baseline: מאה אלף שקל. Top-5: compute ארבעים, DB עשרים וחמש, storage חמש־עשרה, data transfer שתים־עשרה, אחר שמונה. Idle non-prod: שמונה־עשרה אלף. Rightsizing מוצע: עוד שבעת אלפים. לפני שינוי בודקים שלא שוברים latency. Policy חוסמת prod. הדוח מציג עשרים וחמישה אלף פוטנציאליים עם סיכונים. זה איך Decomposition הופך לתוכנית עבודה.

בדוגמת הענן הדגישו שוב את סעיף 5 — בדיקת SLA לפני חיסכון. חיסכון בלי SLA הוא תקרית בהמתנה.

---

# ‏חלק ד׳ — Open / Closed Loop

## ‏11 — Open Loop מול Closed Loop

[סטודנטים · 11]

**על המסך:**

![Open vs Closed Loop](assets/02/03-open-vs-closed-loop.png)

| Open | Closed |
|---|---|
| Plan → ביצוע ליניארי | Plan → Execute → Observe → Re-plan? |
| תהליכים יציבים | עולם עם הפתעות |

**לזכור:** Workflow קבוע עדיף כשאין אי־ודאות וכל הצעדים ידועים מראש.

**להקראה:**

Open Loop מול Closed Loop.

ב-Open Loop התוכנית נקבעת מראש: Plan, אחר כך Step אחד, שניים, שלוש, Done. זה דומה מאוד לאוטומציה קלאסית. מתאים לתהליכים יציבים שבהם המציאות לא מפתיעה.

ב-Closed Loop יש לולאה: Plan, Execute, Observe, Evaluate, ואז החלטה — Re-plan או המשך. Agent מקבל מידע חדש, והמידע הזה יכול לשנות את הצעד הבא.

למה Agents חיים כמעט תמיד ב-Closed Loop? כי API נופל, Data חסר, Tool מחזיר משהו לא צפוי, והבעיה שונה ממה שחשבנו.

משפט לשנן: Plan טוב הוא לא בהכרח Plan שלא משתנה. לפעמים Plan טוב הוא Plan שיודע להשתנות.

ומתי כן Workflow קבוע עדיף על Agent? כשאין אי־ודאות אמיתית, כשכל הצעדים ידועים, כשהסטייה היחידה היא כשל טכני פשוט שפותרים ב-retry או התראה. במקרים האלה Agent הוא סיבוך מיותר.

עוד זווית: Open Loop לא אומר שאין Error Handling.

אפשר Workflow קבוע עם retries ו-alerts. זה עדיין Open מבחינת המסלול העסקי.

Closed Loop אומר שהמסלול העסקי עצמו יכול להשתנות בעקבות Observation. זה ההבדל. לא האם יש try/except, אלא האם יש תכנון מחדש של הדרך.

Closed Loop לא אומר שכל Observation משנה הכול. לפעמים Observation רק מאשר את הצעד הבא בתוכנית. Re-plan הוא הרשאה, לא חובה. המשמעת היא לדעת מתי כן ומתי לא.

Open Loop עדיף גם ברגולציה חזקה: כשצריך להוכיח בדיוק איזה מסלול רץ. Closed Loop עדיף כשהעולם משתנה מהר. יש ארגונים שמשלבים: Open למסלול הכספי, Closed לחקירה טכנית מסביב.

Closed Loop בלי לוג של Re-plan הוא קופסה שחורה. כל שינוי תוכנית חייב להירשם ב-State.

---

## ‏12 — אותה מטרה בשתי גישות

[סטודנטים · 12]

**על המסך:**

מטרה: *״בדוק למה Checkout נכשל ל־3% מהמשתמשים.״*

**Open:** רשימה קבועה Metrics→Logs→DB→CDN→Report (גם אם כבר מצאתם אחרי Logs).  
**Closed:** Metrics → Observation → מקצרים ל־Payments API → Evidence → Stop מוקדם.

הערך: Adaptation חוסך זמן וכסף — אם יש Stop + Budget.

**להקראה:**

אותה מטרה — שתי גישות — נפרט עד הסוף.

המטרה: בדוק למה Checkout נכשל לשלושה אחוז מהמשתמשים.

בגישת Open: רשימה קבועה מראש — Metrics, אחר כך Logs, אחר כך DB, אחר כך CDN, אחר כך Report. גם אם כבר אחרי Logs גיליתם שה-Payments API מחזיר 503 ברור, המערכת עדיין תמשיך ל-DB ול-CDN כי ככה נכתב התסריט. זה צפוי, אבל יקר ואיטי, ולפעמים מבלבל עם רעש נוסף.

בגישת Closed: מתחילים ב-Metrics. רואים כשל מרוכז בנתיב התשלום. Observation מוביל ישר ל-Payments API. מוצאים Evidence. בודקים Success Criteria. עוצרים מוקדם עם דוח ממוקד.

הערך של Adaptation הוא חיסכון בזמן ובכסף — אבל רק אם יש Budget ו-Stop. בלי תקרה, Closed Loop יכול דווקא להסתובב יותר מ-Open. לכן Closed בלי משמעת גבולות הוא לא ארכיטקטורה — זה תקווה.

חשבו על עלות בדוגמת ה-Checkout.

נניח כל בדיקת מערכת עולה עשרים שניות ואלפי Tokens של ניתוח.

ב-Open תמיד תשלמו על חמש תחנות.

ב-Closed עם Stop חכם תשלמו לפעמים על שתיים ותגיעו לאותה המלצה.

אבל ב-Closed בלי Stop תשלמו על שתים־עשרה תחנות מתפתלות. לכן אני חוזר שוב: Adaptation בלי Budget הוא סיכון כלכלי, לא רק טכני.

בדוגמת Checkout, ה-Stop המוקדם חייב להיות מוגדר מראש: למשל נמצא מזהה שירות עם שיעור כשל מעל סף ועם לוג שגיאה חוזר. בלי סף כזה, Closed Loop לא יידע שהוא כבר יכול לעצור.

נחשב גס עלות Tokens. נניח כל תחנת ניתוח עולה שלושים אלף Tokens. Open עם חמש תחנות: מאה וחמישים אלף. Closed שעוצר אחרי שתיים: שישים אלף. ההפרש הוא כסף אמיתי כפול נפח קריאות ביום. לכן ארכיטקטורת Loop היא החלטת תקציב, לא רק החלטת אלגנטיות.

אותה מטרה בשתי גישות מלמדת שבחירת Loop היא החלטת מוצר. תתעדו אותה ב-ADR קצר.

---

# ‏חלק ה׳ — ReAct

## ‏13 — ReAct

[סטודנטים · 13]

**על המסך:**

![ReAct](assets/02/04-react.png)

**הגדרה:** Reason → Act → Observe → חזרה.

יתרון: גמישות. חיסרון: יקר / thrashing בלי תקציב.

**להקראה:**

ReAct.

על המסך הדיאגרמה וההגדרה: Reason, אחר כך Act, אחר כך Observe, ואז חזרה להחלטה.

הרעיון: Agent לא חייב לדעת את כל הדרך מראש. הוא מזהה איזה מידע חסר, קורא לכלי, מסתכל על התוצאה, ומחליט שוב.

היתרון ברור: גמישות כשהבעיה לא ידועה. בדיוק בשביל חקירות.

החיסרון גם ברור: כל החלטה עולה latency ו-Tokens ו-Tool Calls. בלי תקציב איטרציות, ReAct יכול להתפזר — thrashing — ולצרוך תקציב בלי להתקדם.

ReAct הוא כלי. הוא לא דת. משתמשים בו כשהגמישות שווה את המחיר, ומגבים אותו בגבולות.

ReAct מגיע מהרעיון של שילוב הסקה עם פעולה. בלי פעולה יש לכם צ׳אט. בלי הסקה יש לכם אוטומציה עיוורת.

האיזון הוא המשמעת. כל Reason צריך להצדיק למה הכלי הבא שונה ממה שכבר ניסינו. אם אין הצדקה — אל תקראו לכלי. זו משמעת פשוטה שמונעת מחצית מה-thrashing שרואים בשטח.

ReAct לעומת צ׳אט רגיל: בצ׳אט המשתמש הוא ה-Loop. הוא קורא תשובה ומחליט מה לשאול אחר כך. ב-ReAct המערכת היא ה-Loop. היא מחליטה מה הכלי הבא. לכן הגבולות עוברים מהמשתמש אל הארכיטקטורה.

ReAct דורש Observability חזקה כי קשה לשחזר למה נבחר הצעד השביעי. אם אין Trace — אל תשימו ReAct בלב Production קריטי. קודם תשתית תצפית, אחר כך גמישות.

ReAct בלי allow-list של Tools הוא פתח לרחף. הגמישות חייבת להיות בתוך גדר.

---

## ‏14 — ReAct בדוגמה (API 500)

[סטודנטים · 14]

**על המסך:**

```text
Reason: need logs
Act:    get_logs
Observe: DB timeout
Reason: check pool
Act:    get_db_pool_metrics
Observe: pool 95% full
Reason: enough evidence? → report or dig deeper
```

**להקראה:**

דוגמת API שחוזר 500 — נעבור עליה צעד־צעד.

Reason: צריך לוגים, כי בלי לוגים אנחנו מנחשים.

Act: קוראים ל-get_logs.

Observe: Database timeout מופיע בלוג.

עכשיו יש Evidence ראשון. לא סוגרים עדיין, אבל הכיוון השתנה.

Reason: כדאי לבדוק connectivity או connection pool.

Act: get_db_pool_metrics.

Observe: ה-pool כמעט מלא — תשעים וחמישה אחוז.

Reason: יש כיוון חזק. האם מספיק Evidence לדוח, או צריך עוד אישור?

כאן אפשר לעצור עם המלצה לבדוק pool ו-leak אחרי Deploy, או להמשיך לעוד מדד לפני/אחרי. ההחלטה הזו עצמה צריכה להיות מושפעת מ-Budget ומ-Confidence — לא רק מסקרנות של המודל.

שימו לב מה לא קרה: אף אחד לא כתב מראש שאחרי לוגים הולכים דווקא ל-pool. הצעד נולד מהתצפית. זו עוצמת ReAct — וגם הסיכון שלה בלי Stop.

בואו נוסיף סוף אפשרי לדוגמת ה-500.

נניח אחרי pool מלא בדקנו גם Deploy אחרון וראינו שינוי ב-connection pool size. עכשיו Evidence משולב: timeout בלוג, pool רווי, שינוי תצורה קרוב בזמן. Confidence גבוהה יותר.

הדוח יכול להגיד: סיבה סבירה היא לחץ על pool אחרי שינוי תצורה; המלצה — להגדיל pool או לתקן leak; Confidence Medium-High; לא לבצע Restart אוטומטי בלי HITL.

ראו איך הסוף כולל לא רק מה מצאנו — אלא גם מה לא נעשה אוטומטית.

אם אחרי get_logs לא היה timeout אלא Permission denied — כל הסיפור משתנה. לא ממשיכים ל-pool. עוברים ל-Escalate או לכלי עם הרשאה מתאימה. אותו שלד ReAct, ענף אחר לגמרי. זו הסיבה ש-typed observation חשוב.

נוסיף ענף כשל לדוגמה. Act של get_db_pool_metrics נכשל ב-Timeout. typed retry פעם אחת. שוב Timeout. Local re-plan: עבור ל-get_db_connections דרך כלי אחר או Escalate עם הממצאים שכבר יש מ-logs. לא ממשיכים להעמיד פנים שיש pool data.

כשעוברים על רצף ה-500, עצרו רגע אחרי כל Observe — כדי להדגיש את נקודת ההחלטה.

---

## ‏15 — כש-ReAct מתפזר

[סטודנטים · 15]

**על המסך:**

סימנים:
- אותם Tools שוב ושוב  
- Confidence לא עולה  
- Tokens עולים בלי Evidence חדש  

טיפול: Iteration budget · Tool allow-list · ״אין Evidence חדש ×2 → Stop/Escalate״.

**להקראה:**

מתי ReAct מתפזר.

סימן אחד: אותם Tools שוב ושוב. get_logs, שוב get_logs, שוב get_logs — בלי פרמטרים חדשים ובלי Evidence חדש.

סימן שניים: Confidence לא עולה. המודל ממשיך לנחש, אבל לא מצטבר ממצא שמוביל להחלטה.

סימן שלוש: Tokens עולים בלי תמורה. הלולאה רצה, החשבון רץ, התשובה לא מתבגרת.

הטיפול הוא ארכיטקטוני, לא מוסרי.

Iteration budget קשיח. Allow-list של Tools. וכלל פשוט: אם אין Evidence חדש בשני סיבובים רצופים — Stop או Escalate. אפשר גם לדרוש שהמודל יצהיר איזה Evidence חסר לפני עוד Tool Call. בלי משמעת כזאת, ReAct הופך למכונת בזבוז.

דוגמת thrashing מספרית.

Agent עם Budget של שלושים קריאות. בלי Evidence gate הוא יכול לשרוף עשרים קריאות על אותו לוג עם offset שונה במקצת, בלי להעלות Confidence. אחר כך נגמר התקציב בדיוק כשהיה צריך לבדוק מדד קריטי.

עם כלל אין Evidence חדש פעמיים — עוצרים — נשארים עשרים קריאות להמשך חקירה או ל-Escalate מסודר. זה ההבדל בין Agent חקרן לבין Agent בזבזן.

אפשר לממש Evidence gate פשוט: שומרים fingerprint של Observation. אם ה-fingerprint דומה מדי לפעמיים רצופות — מעלים דגל no_progress ועוצרים. לא צריך מודל כדי לזהות תקיעות כזו.

Thrashing קורה גם עם כלים שונים אם הם לא מוסיפים מידע. get_logs, get_events, get_logs שוב עם פילטר חלש — אותה תמונה. Evidence gate צריך לבדוק חדשנות ממצאים, לא רק שם הכלי.

Thrashing הוא גם סימן ל-Prompt גרוע שאומר תמיד תמשיך לחפש. תקנו את ה-Prompt ואת ה-Stop יחד.

---

# ‏חלק ו׳ — Plan-and-Execute

## ‏16 — Plan-and-Execute

[סטודנטים · 16]

**על המסך:**

![Plan-and-Execute](assets/02/05-plan-and-execute.png)

**הגדרה:** Planner בונה תוכנית · Executor מבצע · Re-plan לפי משוב.

```text
1. Inspect deployment
2. Inspect logs
3. Compare release
4. Recommend rollback
```

יתרון: Review / HITL לפני ביצוע מסוכן.

**להקראה:**

Plan-and-Execute.

על המסך הזרימה: Planner בונה תוכנית, Executor מבצע, ואז משוב שיכול להוביל ל-Re-plan.

ההפרדה חשובה ארגונית. Planner עונה על השאלה מה לעשות. Executor עונה על השאלה איך להריץ עכשיו.

הדוגמה על המסך: Inspect deployment, Inspect logs, Compare release, Recommend rollback.

למה זה חזק? כי אפשר לבדוק את התוכנית לפני ביצוע מסוכן. אפשר לתת HITL אחרי Planning ולפני Rollback. אפשר לדבג: האם נכשל ה-Planner או ה-Executor.

המחיר: יותר רכיבים, יותר State, ולפעמים דבקות בתוכנית שגויה אם אין Re-plan אמיתי. Plan-and-Execute בלי לולאת משוב הוא בעצם Open Loop מחופש.

Plan-and-Execute גם מאפשר Plan Validation לפני ריצה.

Completeness: חסר שלב? Validity: האם הצעד אפשרי עם הכלים? Safety: האם יש פעולה מסוכנת? Dependencies: האם הסדר הגיוני? Cost: האם זה יקר מדי?

אפשר Validator דטרמיניסטי, אפשר LLM שני, אפשר אדם. הנקודה היא שיש משטח ביקורת. ב-ReAct טהור המשטח הזה דק יותר, כי התוכנית נולדת תוך כדי תנועה.

Plan-and-Execute לא מונע גמישות. הוא מעביר את הגמישות לנקודות Re-plan מוגדרות. למשל אחרי כל שלושה צעדים, או אחרי כל כשל, או אחרי ממצא עם Confidence גבוה שסותר את התוכנית.

ב-Plan-and-Execute כדאי לשמור את גרסת ה-Plan ב-State. plan_version אחד, אחר כך plan_version שתיים אחרי Re-plan. זה עוזר ב-Debug להבין איזו תוכנית הייתה בתוקף כשכשל Tool מסוים.

Plan-and-Execute מאפשר snapshot של Plan ל-UI משתמש. לפעמים השקיפות הזו שווה את המחיר לבדה.

---

## ‏17 — אותו Incident: ReAct מול Plan-and-Execute

[סטודנטים · 17]

**על המסך:**

| | ReAct | Plan-and-Execute |
|---|---|---|
| צעד ראשון | לפי Thought | לפי Plan שאושר |
| שינוי כיוון | כל Observation | Re-plan מפורש |
| מתאים ל | חקירה פתוחה | סיכון + צורך בביקורת |
| סיכון | מתפזר | נצמד לתוכנית שגויה |

**להקראה:**

אותו Incident — השוואה ישירה בין ReAct ל-Plan-and-Execute.

ב-ReAct הצעד הראשון נקבע לפי Thought רגעי. שינוי כיוון קורה בכל Observation. מתאים לחקירה פתוחה. הסיכון: התפזרות.

ב-Plan-and-Execute הצעד הראשון נקבע לפי Plan שאושר או לפחות נכתב במפורש. שינוי כיוון דורש Re-plan מפורש. מתאים כשיש סיכון וכשצריך ביקורת. הסיכון: היצמדות לתוכנית לא נכונה.

אין מנצח אוניברסלי. אם Rollback עולה כסף רב או פוגע בזמינות — תרצו נקודת ביקורת אחרי Plan. אם אתם בתוך sandbox של לוגים בלבד — ReAct עם Budget יכול להיות מהיר יותר.

השאלה הנכונה היא לא מה יותר AI. השאלה היא מה יותר מתאים לסיכון ולאי־ודאות של המשימה.

כלל בחירה פרקטי:

אם יש פעולת כתיבה או כסף או לקוח בקצה — התחילו Plan-and-Execute עם Gate.

אם יש רק קריאה וחקירה פתוחה — ReAct עם Budget יכול להיות מספיק.

ואם יש גם וגם — היברידי: Planner לרמת־על, ו-ReAct מקומי בתוך צעד חקירה בטוח.

אם אתם בונים מוצר ללקוחות חיצוניים, השמרנות של Plan-and-Execute עם Gate בדרך כלל משתלמת. אם אתם בונים כלי פנימי לחקירת לוגים — ReAct מהיר יותר לשימוש יומיומי. ההקשר העסקי קובע.

טבלת ההשוואה על המסך צריכה להישאר לידכם כדף החלטה. לפני כל Agent חדש: סמנו איזו עמודה מתאימה. אם אתם מסמנים את שתיהן — פרקו את המערכת לתת־משימות עם מדיניות שונה לכל אחת.

אל תבחרו ReAct רק כי Plan-and-Execute נשמע כבד. בחרו לפי סיכון כתיבה ולפי צורך בביקורת.

---

## ‏18 — מתי Planner נפרד?

[סטודנטים · 18]

**על המסך:**

| בלי הפרדה | עם Planner נפרד |
|---|---|
| משימות קצרות | תוכנית ארוכה לבדיקה |
| עלות נמוכה | Policy / Approval אחרי Plan |
| פחות State | Production עם סיכון |

**להקראה:**

מתי Planner נפרד.

בלי הפרדה: משימות קצרות, ReAct פשוט, עלות נמוכה, פחות State. לדוגמה Agent שפותח Ticket לפי תבנית אחרי סיווג קצר.

עם Planner נפרד: תוכנית ארוכה שרוצים לבדוק, Policy Gate, Approval אחרי Plan, או Production עם סיכון. לדוגמה Agent שמציע סדרת שינויי תשתית.

המסר הארכיטקטוני: ההפרדה נגזרת מהסיכון והמורכבות — לא מהטרנד של השוק. אל תבנו Orchestrator עם חמישה Agents רק כי זה נשמע מרשים, אם משימה אחת עם לולאה פשוטה ו-Policy מספיקה.

דוגמת נגד: צוות שבנה Planner Agent, Executor Agent, Critic Agent ו-Supervisor למשימה של סיכום לוג אחד.

הם הוסיפו סדר גודל של latency ועלות, בלי לשפר את איכות הסיכום. זה Multi-Agent theatre.

הפרידו רכיבים כשיש סיבה: ביקורת, סיכון, או מקביליות אמיתית. לא כדי שהדיאגרמה תיראה מרשימה.

סימן שצריך Planner נפרד: אדם רוצה לראות את התוכנית לפני שקורה משהו. אם אף אדם לא מסתכל — אולי ההפרדה מיותרת. אם יש Review — ההפרדה משתלמת.

Planner נפרד גם עוזר בבדיקות. אפשר לבדוק את איכות התוכניות בלי להריץ Tools יקרים. זה יתרון הנדסי גדול בצינורות CI של Agents.

אם Planner נפרד כתוב ב-Python וה-Executor גם — עדיין יכול להיות אותו LLM מאחורי הקלעים. ההפרדה היא חוזית, לא בהכרח מודלית.

---

# ‏חלק ז׳ — Execution ו-Validation

## ‏19 — שלושה סוגי הצלחה

[סטודנטים · 19]

**על המסך:**

![Three kinds of success](assets/02/10-three-success.png)

**הגדרה:** Execution / Validation.

1. Tool success · 2. Step success · 3. Goal success  

‏Tool success ≠ Goal success.

**לזכור:** Tool fail = הכלי נכשל · Task fail = המשימה לא הושגה (גם אם הכלי החזיר 200).

**להקראה:**

שלושה סוגי הצלחה — זה אחד המשפטים החשובים במפגש.

Tool success: הכלי עבד. HTTP מאתיים, תהליך חזר, אין exception.

Step success: קיבלנו מידע שימושי לצעד הנוכחי. לא רק תשובה תקינה טכנית — אלא משהו שמקדם את החקירה.

Goal success: המטרה באמת הושגה. יש סיבה סבירה, או דוח מלא, או פעולה שהושלמה לפי Success Criteria.

והמשפט לשנן: Tool success לא אומר Goal success.

HTTP מאתיים יכול להיות כישלון סמנטי מוחלט. בדיוק לכן צריך Validation בתוך ה-Loop, לא רק try/except סביב ה-Tool.

עוד דוגמה לשלושת הסוגים.

Tool success: search_orders חזר מאתיים.

Step success: מצאנו את ההזמנה הנכונה עם הסכום.

Goal success: החזרנו ללקוח הסבר מדויק על החיוב ופתחנו זיכוי לפי מדיניות.

אפשר להצליח ב-Tool וב-Step ולהיכשל ב-Goal — למשל אם הזיכוי דורש HITL ולא הוגדר Stop נכון, והמערכת דיווחה סיום מוקדם מדי.

במערכות אמיתיות כדאי ללוגג את שלושת הסוגים בנפרד במדדים. אחרת תראו הצלחת Tools גבוהה ותחשבו שהמערכת בריאה, בזמן ש-Goal success בשפל.

הגדירו מדדים: tool_success_rate, step_success_rate, goal_success_rate. אם הראשון גבוה והשלישי נמוך — יש לכם בעיית Validation או Planning, לא בעיית Connectivity.

שלושת סוגי ההצלחה צריכים להופיע באלרטים. אלרט על tool failures בלבד מסתיר קריסת Goal.

---

## ‏20 — Validation — כמה פנים

[סטודנטים · 20]

**על המסך:**

```text
search_tickets() → 200 OK → { "tickets": [] }
```

| מקרה | משמעות |
|---|---|
| 200 + [] | ״לא מצאתי״ — מידע! |
| 200 + schema שבור | Step fail |
| 200 + נתונים לא רלוונטיים | Semantic fail |
| Timeout | Tool fail → typed retry |

**להקראה:**

Validation בכמה פנים — נפרט את הטבלה.

מקרה אחד: מאתיים עם רשימת tickets ריקה. מבחינת Transport ו-Schema הכול תקין. סמנטית זה אומר לא מצאתי. זה מידע חשוב. זה לא הכל בסדר.

מקרה שניים: מאתיים עם schema שבור. Step fail. אל תמשיכו כאילו יש לכם נתונים.

מקרה שלוש: מאתיים עם נתונים לא רלוונטיים. למשל חיפשתם לקוח א אבל קיבלתם לקוח ב בגלל פרמטר שגוי. Semantic fail. צריך Fix args או Re-plan — לא לחגוג הצלחה.

מקרה ארבע: Timeout. Tool fail. כאן typed retry עם backoff יכול להיות נכון.

Validation הוא לא נימוס. הוא ההבדל בין Agent שרץ לבין Agent שמבין מה רץ.

איך מממשים Semantic Validation בפועל.

אחרי כל Tool: בדיקת schema. אחר כך בדיקת emptiness לפי ההקשר. אחר כך בדיקת relevance — האם הישות שחזרה תואמת את ה-ID שביקשנו. רק אז מעדכנים findings ב-State.

אם אחת הבדיקות נכשלת — לא זורמים לסיכום בביטחון גבוה. מעדכנים Confidence כלפי מטה או יוצרים Step של תיקון פרמטרים.

Empty result דורש פרשנות לפי כוונה. אם חיפשתם תקלה ידועה ולא מצאתם — אולי באמת אין. אם חיפשתם לקוח חובה במערכת ואין — זו שגיאת נתונים או פרמטר. אותו JSON ריק, שתי משמעויות. Semantic Validation תלוית הקשר.

דוגמה נוספת ל-Semantic fail: חיפשתם user_id 55 וקיבלתם משתמש 55 אבל מסביבה אחרת. Schema תקין, ID תואם, ובכל זאת Wrong environment. Validation חייב לכלול גם הקשר סביבה.

Semantic Validation אפשר לבדוק עם טסטים קטנים בלי LLM: תנו פיxture של JSON ריק וודאו שהמערכת לא מסמנת Goal success.

---

# ‏חלק ח׳ — Failure וגבולות

## ‏21 — Failure Handling

[סטודנטים · 21]

**על המסך:**

![Failure Handling](assets/02/06-failure-handling.png)

Timeout · Auth · Bad params · Empty · Rate limit · Unexpected · Model mistake

טיפול: typed retry · כלי חלופי · Local/Global re-plan · Escalation

**להקראה:**

Failure Handling.

בעולם אמיתי Agent ייתקל ב-Timeout, Auth failure, פרמטרים שגויים, תוצאה ריקה, Rate limit, שגיאת רשת, Data לא צפוי, וטעות מודל.

Failure Handling צריך להיות חלק מהארכיטקטורה מהיום הראשון — לא משהו שמוסיפים אחרי תקלה ביום שישי בלילה.

התפריט הבסיסי: typed retry, כלי חלופי, Local re-plan, Global re-plan, ו-Escalation לאדם.

Retry עיוור הוא אנטי־דפוס. אם הכלי אומר Invalid customer_id, להריץ שוב אותו דבר לא יהפוך את ה-ID לתקין. זה רק יוסיף עומס.

ספרו את רשימת הכשלים כתפריט תכנון, לא כסיפור אימה.

לכל סוג כשל צריך להיות Owner בארכיטקטורה: מי מטפל, כמה פעמים, מה ה-fallback, ומתי אדם. אם אין Owner — הכשל יטופל באימפרוביזציה של המודל, וזה בדיוק מה שלא רוצים בפרודקשן.

Model mistake הוא כשל שקל לשכוח. המודל יכול לבחור Tool נכון עם ארגומנטים שגויים שוב ושוב. לכן מגבילים Retries על invalid parameter, ודורשים שינוי ארגומנטים בין ניסיונות — לא אותו קריאה חוזרת.

סדרו את הטיפול בכשלים כטבלת Routing בקוד. לא כפסקה ב-Prompt. Prompt יכול להסביר, קוד חייב לאכוף.

Failure Handling בלי Escalation path הוא בור. מישהו חייב לקבל את התיק כשהמערכת עוצרת.

---

## ‏22 — מטריצת Retry + Backoff

[סטודנטים · 22]

**על המסך:**

```text
Timeout           → Retry + backoff
Rate limit        → Wait + Retry
Invalid parameter → Fix args + Retry once
Permission denied → Stop / Escalate
Dangerous op      → HITL
Empty useful data → Re-plan (not blind retry)
```

```text
Backoff: 1s → 2s → 4s → 8s (עם תקרה)
```

**להקראה:**

מטריצת Retry ו-Backoff — נקרא אותה לאט.

Timeout: Retry עם backoff. ייתכן שהשירות חזר.

Rate limit: Wait ואז Retry. לא ללחוץ חזק יותר.

Invalid parameter: Fix args ואז Retry פעם אחת. אם נכשל שוב — Re-plan או Escalate.

Permission denied: Stop או Escalate. לא Retry. אין קסם שיפתח הרשאה.

Dangerous operation: HITL. נקודה.

Empty useful data: Re-plan — לא blind retry. תוצאה ריקה יכולה להיות Evidence לגיטימי.

Backoff לדוגמה: שנייה, שתיים, ארבע, שמונה, עם תקרה. זה Software Engineering קלאסי שצריך לשבת בתוך Agent Loop, לא לידה כהערת שוליים.

דוגמה רעה: Agent קיבל Rate limit ומיד ירה עשרים Retries צפופים. הוא הפך את עצמו למתקפת עומס.

דוגמה טובה: Rate limit → Wait לפי הודעת השרת או backoff → Retry מוגבל → אם נמשך — Escalate או כלי חלופי.

אותה תשתית, התנהגות הפוכה. ההבדל הוא המטריצה שעל המסך כשהיא באמת מחוברת לקוד.

Backoff עם jitter חשוב ב-Production כדי שלא כל ה-Agents יתעוררו באותה שנייה. זו עוד דוגמה לכך ש-Agent systems הם מערכות מבוזרות, לא קסם של מודל.

Empty useful data מול Empty because error: אם הכלי אומר success עם רשימה ריקה — זה Observation. אם הכלי זורק — זה Failure. אל תטפלו בשניהם באותו מסלול Retry.

Backoff בלי תקרה עליונה גם הוא לולאה. תמיד max_retries ו-max_delay.

---

## ‏23 — Budget, Stop, Success Criteria

[סטודנטים · 23]

**על המסך:**

**הגדרה:** Iteration Limit · Stop Condition.

דוגמה למשימת דוח:
- Success: קובץ + 4 סעיפים + נתונים נבדקו  
- Fail: אין גישה למקור > 2 ניסיונות  
- Stop: 12 tool calls / 5 דק׳ / Confidence גבוהה מספיק  

**לזכור:** בלי תקרה Agent יכול לשרוף Tokens/כסף/זמן בלי סוף.

**להקראה:**

Budget, Stop, Success Criteria.

Iteration Limit הוא תקרה על מחזורים, Tool calls, או Tokens. בלי תקרה Agent יכול לשרוף זמן וכסף בלי סוף.

Stop Condition מגדיר מתי עוצרים: הצלחה, כישלון, Timeout, Escalation, או Confidence מספיק.

ניקח את דוגמת הדוח על המסך ונפרט.

Success: הקובץ קיים, יש ארבעה סעיפים, והנתונים נבדקו. לא מספיק שהמודל אמר סיימתי.

Fail: אין גישה למקור אחרי שני ניסיונות מתוקנים.

Stop נוסף: שתים־עשרה קריאות Tool, או חמש דקות, או Confidence גבוהה מספיק כדי למסור טיוטה עם הסתייגות.

Success Criteria הוא חוזה. בלי חוזה, Agent תמיד יכול להמשיך עוד קצת — וזה בדיוק איך נגמרות לולאות יקרות.

איך כותבים Success Criteria טוב.

הימנעו ממילים כמו נכון או מספיק בלי מדידה. העדיפו: קובץ קיים בנתיב X, כולל סעיפים A B C D, כל סעיף עם לפחות מקור אחד, וסטטוס בדיקה עבר.

לכישלון: גם הוא חייב להיות מוגדר. אחרת Agent לא יודע מתי להיכנע, והוא ימשיך לחפש לנצח.

Budget יכול להיות מדורג: רך וקשיח. רך — אזהרה ב-State והעדפת כלים זולים. קשיח — Stop. כך המערכת לא מתה בהפתעה, אלא קודם מנסה להיסגר בזול.

Success Criteria לדוגמת CI: נמצאה שורת שגיאה מפענחת או שינוי בקוד שמסביר את הכשל, ונכתבה המלצה אחת אופרטיבית. Fail Criteria: אין גישה ללוג ולשינויים אחרי שני מסלולים. ברור. מדיד. ניתן ליישום.

Budget שמופיע רק בדוקומנטציה לא קיים. Budget חי ב-State ובקוד שעוצר.

---

# ‏חלק ט׳ — מבנה והחלטות

## ‏24 — Dependencies ו-Branches

[סטודנטים · 24]

**על המסך:**

**הגדרה:** Parallel Execution.

- מקבילי כשאין תלות · זהירות מ-rate limits  
- Branches: `severity==high` → HITL (עדיף בקוד)

**להקראה:**

Dependencies ו-Branches.

לא הכול חייב להיות סדרתי. אם שלושה חיפושים לא תלויים זה בזה — אפשר מקבילית, ואז Aggregation. זה חוסך זמן.

אבל Parallelization מוסיף מורכבות: rate limits, כשל חלקי, סדר תוצאות, ומירוצים. לא עושים מקבילית רק כי אפשר.

Branches: אם severity גבוה — HITL. אם נמוך — טיפול אוטומטי. את זיהוי ה-severity אפשר לתת למודל. את המדיניות עצמה — עדיף בקוד. אחרת המודל עלול להחליט ש-critical הוא בעצם בסדר היום.

דוגמת Branch מסוכנת.

המודל מסווג severity כ-low כי הלקוח נימוס בניסוח. הקוד מאמין למודל ומבצע Refund אוטומטי מעל תקרה.

התיקון: המודל יכול להציע severity, אבל כלל סכום מעל X תמיד HITL נשאר דטרמיניסטי. פרשנות לא מקבלת רשות לעקוף כסף.

Aggregation אחרי מקביליות חייב להיות מפורש: מה קורה אם שניים הצליחו ואחד נכשל. האם ממשיכים עם חלקי? האם Escalation? בלי כלל — המודל יאלתר, ואלתור במקביליות הוא באג בהמתנה.

Parallelization בחקירה: אפשר metrics ו-deploy history במקביל. לא כדאי להריץ במקביל פעולת write עם חקירה. הפרירו קריאה לכתיבה גם ברמת התזמון.

Branches צריך לבדוק עם טבלאות החלטה: קלט severity/סכום/סביבה → פלט auto או HITL.

---

## ‏25 — קוד דטרמיניסטי מול LLM

[סטודנטים · 25]

**על המסך:**

| בקוד | במודל |
|---|---|
| Policy, timeouts, retries טכניים | בחירת כיוון, השערות, סיכום |
| Allow-list של Tools | ניסוח / סיווג |

```python
if severity == "critical":
    require_human_approval()
```

**להקראה:**

קוד דטרמיניסטי מול LLM.

בטבלה: בקוד — Policy, timeouts, retries טכניים, allow-list. במודל — בחירת כיוון, השערות, סיכום, סיווג רך.

הקוד על המסך:

אם severity שווה critical — דורשים אישור אדם.

אין כאן צורך שהמודל יהיה יצירתי. המדיניות כבר ידועה. לשים אותה ב-Prompt במקום בקוד זה להזמין דרך עוקפת.

אנחנו לא בונים AI במקום תוכנה. אנחנו בונים AI בתוך מערכת תוכנה.

גבול נוסף לקוד: allow-list של Tools לפי סביבה.

ב-Production Investigation: רק read tools. ב-Staging: אפשר restart. ב-Local: אפשר יותר.

המודל בוחר מתוך הרשימה שניתנה לו — לא ממציא Tool חדש ולא מרחיב הרשאות. זו אחת משכבות האבטחה הזולות והחזקות ביותר.

גם ניסוח ללקוח יכול להישאר במודל, בזמן שההחלטה אם מותר לשלוח נשארת בקוד. הפרדה בין Generation לבין Authorization. בלבל ביניהם — ותקבלו מערכת שמפתה לדבר יפה בזמן שהיא עושה דברים אסורים.

שמרו את מדיניות critical בקוד גם אם המודל מתחנן בטקסט שיש לו Evidence מושלם. טקסט לא פותח מנעול. Approval id פותח.

הפרדת קוד/מודל היא גם הפרדת אחריות בעלות: מי מסביר ללקוח מול מי מאשר פעולה.

---

## ‏26 — דוגמאות החלטה: קוד או מודל?

[סטודנטים · 26]

**על המסך:**

1. האם Restart מותר בלי אישור בלילה? → **קוד/Policy**  
2. איזה שירות לבדוק קודם לפי תסמינים? → **מודל**  
3. האם JSON תקין? → **קוד**  
4. האם הממצאים מספיקים להמלצת Rollback? → **מודל + כללי Confidence**

**להקראה:**

ארבע דוגמאות החלטה — נענה עליהן אחת־אחת בהדגמה.

אחת: האם Restart מותר בלי אישור בלילה? זו מדיניות ארגונית. היא שייכת לקוד או ל-Policy Engine — לא להחלטה חופשית של המודל בכל הרצה.

שתיים: איזה שירות לבדוק קודם לפי תסמינים? כאן יש פרשנות. זה מתאים למודל בתוך רשימת שירותים מותרת.

שלוש: האם JSON תקין? בדיקה דטרמיניסטית. קוד. תמיד.

ארבע: האם הממצאים מספיקים להמלצת Rollback? שיפוט עם Evidence. מודל יכול להציע, אבל כללי Confidence ו-HITL צריכים להיות מחוץ לגחמה של Prompt בודד.

הדפוס: חשיבה גמישה למודל, גבולות קשיחים לקוד.

אם אתם זוכרים רק דוגמה אחת מהשקופית הזו — זכרו את Restart בלילה.

זו החלטה ארגונית שצריכה להיות כתובה במדיניות, נבדקת בקוד, ומתועדת ב-Trace. לא ויכוח מחדש בכל Incident עם המודל.

סעיף ארבע — Rollback — הוא מקום שבו Confidence חייב להיות מחובר ל-HITL. גם אם המודל בטוח מאוד, Rollback בפרודקשן הוא אירוע ניהולי. הביטחון של המודל לא מחליף Owner.

עברו שוב על ארבע הדוגמאות בקול כרשימת תיוג. אחת קוד. שתיים מודל. שלוש קוד. ארבע מודל פלוס HITL. זה הקצב שצריך להישאר בראש אחרי המפגש.

אם הדוגמאות הארבע לא ברורות אחרי הקראה אחת — חזרו עליהן. הן לב המפגש הארגוני.

---

# ‏חלק י׳ — Incident, Demo, Lab חי

## ‏27 — Incident Agent (מלא)

[סטודנטים · 27]

**על המסך:**

![Incident Agent](assets/02/09-incident-agent.png)

מטרה: latency גבוה ב-API · סיבה סבירה · המלצה.  
Tools קריאה בלבד: metrics · logs · deployments · incidents

זרימה: Confirm → Correlate deploy → Logs → Re-plan לפי Evidence

![Metrics](assets/screens/metrics-dashboard.png)

![Kubernetes](assets/screens/k8s-pods-logs.png)

**להקראה:**

Incident Agent מלא.

על המסך הדיאגרמה וצילומי Metrics ו-Kubernetes.

המטרה: latency גבוה ב-API, מצא סיבה סבירה, תן המלצה.

בשלב האבחון Tools הם קריאה בלבד: metrics, logs, deployments, incidents. אין restart, אין deploy, אין delete. זה Pattern שכדאי לשנן: Investigate כלים הם subset של read-only.

הזרימה: Confirm שיש בעיה, Correlate מול deploy, לוגים, ואז Re-plan לפי Evidence.

לפני שרצים לכלים — אומרים בקול את המטרה ואת הגבולות. אחרת Agent חכם עם הרשאות רחבות יהפוך מהר מאוד לסיכון תפעולי.

לפני שנכנסים למסכים: אמרו בקול את משפט הגבול.

היום אנחנו רק מאבחנים. אנחנו לא מתקנים. אנחנו לא מריסטארטים. אנחנו לא מוחקים.

המשפט הזה משנה את כל ה-Plan. בלי אותו משפט, המודל ישאף לסגור את המעגל עם פעולה, כי כך הוא אומן — לסיים משימות.

כשמסתכלים על מסך ה-Metrics, חשוב לשאול בקול מה מחפשים: מתי התחיל, כמה חד, האם זה בכל ה-instances או בחלק. השאלות האלה הן Planning בתוך הראש לפני Tool Call.

אם תוסיפו לתמונה Tool של restart כבר עכשיו — תשנו את כל ה-Design Review בלי להרגיש. לכן בשלב האבחון אני חוזר ואומר: רק read. משמעת הכלים היא משמעת המטרה.

Incident Agent read-only הוא תבנית שאפשר לאמץ מחר בלי פחד. התחילו משם בכל מערכת חדשה.

---

## ‏28 — צעדים + Re-plan בדוגמה

[סטודנטים · 28]

**על המסך:**

```text
get_metrics     → p95 80ms→620ms (מאושר)
get_deployments → 3.8.1 לפני 32 דק׳ (correlation ≠ proof)
get_logs        → DB timeout
REPLAN          → pool metrics + before/after + prior incidents
```

Workflow קבוע היה ממשיך גם אחרי שיש תשובה. Agent יכול Stop מוקדם.

**להקראה:**

נפרט את הצעדים.

get_metrics: p95 עלה משמונים מילישניות לשש מאות עשרים. הבעיה מאושרת, לא דמיון של Alert.

get_deployments: גרסה 3.8.1 עלתה לפני בערך שלושים ושתיים דקות. יש Correlation בזמן. זה עדיין לא Proof. יכול להיות Coincidence.

get_logs: Database timeout. עכשיו יש Evidence נוסף שמחבר latency ל-DB.

Re-plan: במקום להמשיך ברשימה הגנרית, עוברים ל-pool metrics, before/after release, ו-prior incidents. זו ליבת ה-Closed Loop.

Workflow קבוע היה ממשיך גם אחרי שיש תשובה מספיק טובה. Agent טוב יכול לעצור מוקדם — אם הוגדר לו מה נחשב מספיק.

איך הייתי מנסח את הדוח בסוף הדוגמה הזו.

מצאנו עלייה חדה ב-p95. יש Correlation עם Deploy 3.8.1. יש Evidence של DB timeout ו-pool רווי. Confidence בינוני-גבוה שהבעיה קשורה ללחץ על חיבורי DB אחרי השחרור. המלצה: לבדוק pool ו-queries חריגים; לא לבצע Rollback אוטומטי בלי אדם. מה חסר: מדד pool לפני/אחרי וזמן מדויק של שינוי תצורה.

ראו את המבנה: ממצאים, Confidence, המלצה, גבולות, וחסרים. זה דוח Agent בוגר.

Correlation ≠ Proof — תתרגלו להגיד את זה בקול גם כשזה לא נוח. כדאי לשמוע את המשמעת הזו מספיק פעמים כדי שתישאר.

אחרי הדוגמה הזו אתם אמורים להיות מסוגלים לכתוב Trace מדומה: זמן, כלי, תצפית, Hypothesis מעודכנת, Confidence. אם לא — עצרו והקשיבו שוב לצעדים.

כתבו את ארבעת הצעדים כקובץ YAML לדוגמה. זה יהפוך את הסיפור לנכס רב־שימוש.

---

## ‏29 — הדגמה CI + Pseudocode

[סטודנטים · 29]

**על המסך:**

![CI console](assets/screens/ci-console.png)

![Demo plan execute](assets/02/demo-plan-execute.png)

```python
goal = user_request
while not done:
    decision = llm(goal=goal, state=state, tools=available_tools)
    result = execute(decision)
    state = update(state, result)
    done = check_stop(state)
```

**להקראה:**

הדגמת CI על המסך, ואז Pseudocode של הלולאה.

הלולאה: כל עוד לא done — המודל מחליט לפי goal ו-state ו-tools, מבצעים, מעדכנים state, בודקים stop.

זה השלד. ב-Production חסרים עליו שכבות: Policy, budgets, idempotency, tracing, timeouts. בלי השכבות האלה יש לכם דמו מרשים ולא מערכת.

נעבור עוד רגע ל-Lab חי שבו רואים את השלד הזה רץ מול AWS אמיתי, ואז נחזור להדגמות פתרון מלאות.

כשמסתכלים על ה-Pseudocode, עצרו על השורה check_stop.

זו השורה שהכי הרבה דמואים שוכחים לממש באמת. הם שמים done שווה True כשהמודל אומר finished. זה לא Stop Condition — זה משאלת לב.

Stop צריך לבדוק קריטריונים כתובים, תקציב, שגיאות חוזרות, ודגלים של Policy. המודל יכול להציע סיום; המערכת מחליטה אם מותר לסיים.

חסר נוסף ב-Pseudocode: טיפול ב-Policy לפני execute. ההחלטה של המודל היא הצעה. execute בודק הרשאה. בלי זה אתם מריצים את המודל כאדמין.

ה-Pseudocode הוא חוזה מינימלי. כל מערכת אמיתית תרחיב אותו. אבל אם המערכת שלכם לא יכולה להיות ממופה ללולאה הזו — כנראה שהיא Workflow מחופש או צ׳אט עם תוסף.

check_stop הוא המקום לחבר Success Criteria אמיתיים. אל תאצילו אותו למודל לבדו.

---

## ‏30 — Lab חי: מטרה ומגבלות

[סטודנטים · 30]

**על המסך:**

**מחוץ למצגת / לצד המצגת:** Claude Code + AWS.

מטרה: לאשר גישה ל-AWS ולדגום אם יש מדדי EC2 CPU (read-only).

מגבלות להדגמה:
- אזור אחד · בלי Create/Update/Delete  
- `MAX_ITERATIONS = 5`  
- תקציב: סנטים  

קוד: `labs/02-aws-agent-loop/`

**להקראה:**

Lab חי — מטרה ומגבלות.

אנחנו יוצאים קצת מהמצגת אל המסוף או אל Claude Code — והמגבלות נשארות על המסך.

המטרה: לאשר גישה ל-AWS ולדגום אם יש מדדי EC2 CPU. קריאה בלבד.

מגבלות: אזור אחד, בלי Create Update Delete, מקסימום חמש איטרציות, תקציב של סנטים.

הקוד נמצא תחת labs/02-aws-agent-loop. הפרופיל לדוגמה הוא nashpazformatan והאזור eu-north-1.

המטרה הפדגוגית של ה-Lab: לא להרשים עם תשתית. להראות Plan, Observation, ו-Re-plan על תוצאה ריקה או חלקית.

למה Lab זול וקטן עדיף כאן על דמו מפלצתי.

כי אנחנו רוצים לראות את הלולאה, לא את התשתית. אם נשקיע עשרים דקות בהקמת קלאסטר, נאבד את השיעור.

המגבלה read-only היא גם שיעור מוסרי-הנדסי: קודם הוכיחו אבחון, אחר כך דיברו על תיקון.

לפני המעבר למסוף נבהיר מה לא יקרה: לא ניצור שרתים, לא נפתח Security Groups, לא נשאיר משאבים דולקים. זה גם שיעור ב-Policy.

---

## ‏31 — Lab חי: מה נריץ

[סטודנטים · 31]

**על המסך:**

```bash
cd labs/02-aws-agent-loop
export AWS_PROFILE=nashpazformatan
export AWS_REGION=eu-north-1
python3 agent_loop.py
```

שימו לב בלייב:
1. Initial plan  
2. Observation מ-STS / CloudWatch  
3. Local re-plan אם 0 נקודות מדד  
4. Stop + findings  

אופציונלי: לשנות Plan ב-Claude Code ולהריץ שוב.

**להקראה:**

Lab חי — מה נריץ.

במסוף:

cd labs/02-aws-agent-loop

export AWS_PROFILE=nashpazformatan

export AWS_REGION=eu-north-1

python3 agent_loop.py

עכשיו בקול בזמן הריצה: מה ה-Plan הראשוני, מה חוזר מ-STS, מה חוזר מ-CloudWatch, ומתי הקוד עושה Local re-plan.

אם CloudWatch מחזיר אפס נקודות — זה לא כישלון הוראה. זה Scenario מצוין: Observation ריק הוא Evidence, ואז מדלגים להמשך ומסכמים.

אם credentials נכשלים — גם זה שיעור: Auth failure הולך ל-Stop או Escalate, לא ל-Retry אינסופי. אפשר להצביע על הענף הזה בקוד.

בזמן הריצה, narrate כמו פרשן משחק.

הנה Plan. הנה Tool ראשון. הנה Observation. האם זה מקדם Goal. האם צריך Re-plan. האם Stop.

אם תתרגלו לספר כך גם כשאתם מדבגים מערכות אמיתיות — תתפסו מהר יותר איפה ה-Loop שבור.

אחרי הריצה, גלגלו חזרה לפלט והקריאו את findings שורה־שורה. זה מחבר את ה-Lab לשפת State ו-Evidence שלמדנו.

אם הפלט ארוך, הקריאו את החלקים: GOAL, INITIAL PLAN, OBSERVE, PLAN NOW, STOP. זו שפת המפגש על מסך טרמינל.

אם STS עובר ו-CloudWatch ריק — ספרו את זה כהצלחת תרחיש, לא ככישלון דמו.

---

## ‏32 — Lab חי: Debrief

[סטודנטים · 32]

**על המסך:**

מה ראינו?
- Plan ≠ חוזה  
- Observation ריק עדיין Evidence  
- Re-plan מקומי זול מ-Global  
- Stop condition עצר לפני לולאה מיותרת  

**לזכור אחרי Lab:** allow-list של Tools · max iterations · אין write בלי HITL.

**להקראה:**

Lab חי — Debrief.

מה ראינו בפועל.

Plan אינו חוזה. הוא Hypothesis שרצה מול עולם.

Observation ריק עדיין Evidence. הוא אומר אין מדדים כאן, לא הכל בסדר וגם לא בהכרח הכל שבור.

Local re-plan היה זול: שינוי המשך התוכנית בלי לזרוק את כל המסגרת.

Stop condition עצר לפני לולאה מיותרת.

לפני Production היינו מוסיפים ל-Policy לפחות: allow-list של Tools, max iterations, ואיסור מוחלט על write בלי HITL. בדיוק את זה נראה שוב בהדגמות הפתרון בהמשך.

שלושה Takeaways מה-Lab לשים בכיס.

אחד: ריק זה Evidence. שניים: Local re-plan הוא ברירת מחדל זולה. שלוש: Policy write נשאר סגור עד שמישהו פותח אותו במודע.

אם רק את השלושה האלה תיישמו במערכת הבאה שלכם — כבר שיפרתם את רוב הדמואים שרצים בלי משמעת.

Debrief טוב נגמר במשפט יישום: מחר בבוקר, ב-Agent שלכם, הוסיפו Evidence gate אחד ו-Stop אחד. לא עשרה פיצ׳רים — שניים שמשנים התנהגות.

Debrief נגמר בפעולה אחת ליישום: הוסיפו Stop condition אחד למערכת שלכם השבוע.

---

# ‏חלק יא׳ — Evidence, Policy, State

## ‏33 — Evidence, Hypothesis, Confidence

[סטודנטים · 33]

**על המסך:**

```text
Hypothesis → Evidence → Supports? → ↑confidence / החלף Hypothesis
```

Correlation ≠ Proof. Low / Medium / High.

**להקראה:**

Evidence, Hypothesis, Confidence.

הלולאה על המסך: Hypothesis, אחר כך Evidence, אחר כך השאלה Supports, ואז או מעלים Confidence או מחליפים Hypothesis.

אל תתאהבו בהשערה הראשונה. Deploy עלה ו-Latency עלה — קל להגיד ה-Deploy אשם. זה Correlation. זה עדיין לא Proof.

Confidence יכול להיות Low, Medium, High. לא חובה מספר מדויק עם שתי ספרות אחרי הנקודה. חובה יושרה: מה תומך, מה חסר, ומה ההמלצה תחת אי־הוודאות שנשארה.

תרגיל חשיבה מוקלט — אני עונה עליו מיד.

Deploy עלה, Latency עלה, אבל לוגים נקיים ו-DB בריא. מה עכשיו?

לא סוגרים שזה ה-Deploy. מחפשים Evidence נוסף: תלות חיצונית, CDN, feature flag, או עומס משתמשים. Confidence נשאר נמוך עד שיש תומכים. זו המשמעת של Hypothesis Loop.

Confidence גבוה בלי Evidence הוא באג. Confidence נמוך עם Evidence חזק הוא גם באג — פספוס של Stop. תכיילו את השפה הזו מול דוגמאות אמיתיות מהעבודה שלכם.

Hypothesis Loop הוא גם כלי תקשורת לצוות. במקום להגיד נראה לי, אומרים Hypothesis, Evidence, Confidence. זה מקצר ויכוחים בזמן תקרית.

Evidence בלי מקור הוא רכילות. שמרו ליד כל ממצא את שם הכלי והזמן.

---

## ‏34 — Wrong Assumption (Release)

[סטודנטים · 34]

**על המסך:**

Agent מניח: `Build → Test → Deploy`  
ארגון דורש: Scan → Approvals → Staging → Prod Approval.

בלי Constraints התוכנית ״עובדת״ במעבדה ונכשלת בפרוד.

**להקראה:**

Wrong Assumption ב-Release.

Agent מניח: Build, אחר כך Test, אחר כך Deploy.

הארגון דורש: Security Scan, Approvals, Staging, Integration, Production Approval, ורק אז Production.

בלי Constraints התוכנית נראית חכמה במעבדה ונכשלת בפרודקשן. זו לא בעיית מודל dumb. זו בעיית הקשר חסר.

לכן Planning בלי שכבת אילוצים הוא Planning חלקי. המטרה לבד לא מספיקה. צריך את חוקי המשחק של הסביבה.

איך מונעים Wrong Assumption ארגוני.

מזינים ל-Planner קטלוג Constraints: סביבות, חלונות שינוי, Approvals חובה, רשימת שלבים ל-Release. לא מניחים שהמודל מכיר את החברה שלכם. הוא לא מכיר.

Wrong assumption לא חי רק ב-Release. הוא חי גם ב-Support: המודל מניח שהלקוח טעה בהקלדה, בזמן שהחיוב באמת כפול. Constraints של Billing — למשל חובה לבדוק ledger לפני תשובה — מונעים את זה.

הכניסו Constraints לקובץ קונפיג או לשרת מדיניות — לא רק לשיחה עם המודל. מה שלא בקונפיג, לא קיים באמת.

Wrong assumption נפתר בקונפיג, בטסטים, וב-Review — לא בתקווה שהמודל ינחש נכון.

---

## ‏35 — Local מול Global Re-plan

[סטודנטים · 35]

**על המסך:**

**הגדרה:** Re-planning.

| Local | Global |
|---|---|
| צעד נכשל → חלופה | זורקים מסגרת חקירה |
| זול | יקר |

**לזכור:** Local כשנשברת רק פעולה; Global כשנשברת מסגרת החקירה.

**להקראה:**

Local מול Global Re-plan.

Local: הצעד הנוכחי נכשל או התרוקן — בוחרים חלופה. זול ומהיר. זה מה שראינו ב-Lab כשלא היו מדדים.

Global: Evidence גדול משנה את מסגרת החקירה — זורקים תוכנית ובונים חדשה. יקר יותר, ולפעמים הכרחי.

כלל מעשי: קודם Local. Global רק כשהשאלה עצמה השתנתה. למשל חשבנו שזה Runner, וגילינו שזה בכלל Pipeline אחר או שירות אחר.

Re-plan לא בכל צעד. Re-plan כשיש סיבה. אחרת חוזרים ל-thrashing יקר.

דוגמת גבול בין Local ל-Global.

Local: Tool של לוגים נכשל → עבור לאירועי מערכת.

Global: גילינו שהתקלה בכלל בשירות אחר ממה שהמשתמש דיווח → תוכנית חקירה חדשה עם יעד חדש ב-State.

אם תעשו Global על כל Timeout קטן — תשרפו תקציב. אם לא תעשו Global כשהיעד השתנה — תחקרו את השירות הלא נכון ביסודיות מרשימה.

אפשר לקודד כלל: אם היעד ב-State השתנה — חובה Global. אם רק הצעד נכשל — Local. זה מונע ויכוחים פילוסופיים בזמן Incident.

Local נכשל אם אין חלופה. אז Escalate או Global. אל תמציאו חלופות מסוכנות רק כדי להימנע מ-Escalate.

Global re-plan חייב לאפס current_step ולשמור findings ישנים. אל תמחקו ידע.

---

## ‏36 — Constraints ו-Policy Gate

[סטודנטים · 36]

**על המסך:**

![Policy Gate](assets/02/07-policy-gate.png)

![Approval dialog](assets/screens/approval-dialog.png)

**הגדרה:** Constraint / Policy.

זמן · עלות · הרשאות · ״אין נגיעה בפרוד בלי אישור״

**להקראה:**

Constraints ו-Policy Gate.

על המסך הדיאגרמה ודיאלוג האישור.

מטרה בלי אילוצים לא מספיקה: זמן, עלות, הרשאות, ואיסור נגיעה בפרוד בלי אישור.

Policy Gate עומדת בין Plan לבין Execution. התוכנית יכולה להיות מבריקה — ועדיין להיחסם אם היא עוברת קו אדום.

Approval באמצע תהליך דורש State. אחרת אחרי האישור אתם מתחילים מאפס, מאבדים הקשר, ויוצרים סיכון לביצוע כפול.

Policy Gate לא חייב להיות UI מורכב.

לפעמים זה פונקציה אחת: אם tool בקבוצת dangerous ואין approval_id ב-State — זרוק PermissionError מבוקר והפעל Escalation.

הפשטות הזו עדיפה על פני פסקה ארוכה ב-Prompt שאומרת בבקשה אל תעשה דברים מסוכנים.

Approval חייב להיות Idempotent מצד עצמו. לחיצה כפולה על Approve לא צריכה להפעיל פעולה פעמיים. זה מתחבר ישירות לשקופית Idempotency.

Policy Gate צריך להיות Auditable: מי ביקש, מה אושר, מתי, לאיזה plan_version. בלי זה אין משילות.

Policy Gate הוא המקום שבו משפטים כמו אסור בפרוד הופכים לקוד שרץ.

---

## ‏37 — Execution State

[סטודנטים · 37]

**על המסך:**

![Execution State](assets/02/08-execution-state.png)

**הגדרה:** Execution State.

```json
{
  "goal": "Investigate API latency",
  "current_step": "check_logs",
  "completed_steps": ["get_metrics", "get_deployments"],
  "failed_steps": [],
  "findings": [
    "latency increased 32 minutes ago",
    "deployment 3.8.1 happened 35 minutes ago"
  ]
}
```

**להקראה:**

Execution State.

על המסך ה-JSON לדוגמה.

רואים goal, current_step, completed_steps, failed_steps, findings.

זה החוזה של המערכת עם עצמה בזמן ריצה. בלי State קשה לעשות Retry חכם, Resume אחרי HITL, Re-planning, ו-Debug אחרי תקלה.

בהמשך הקורס נעמיק ב-Memory. היום מספיק להבין: State הוא לא לוג מזדמן. הוא רכיב ארכיטקטוני.

מה עוד כדאי לשמור ב-State מעבר לדוגמה.

timestamps של כל צעד, מספר retries לפי סוג שגיאה, רשימת tools שכבר כשלו, ו-budget_remaining. אלה הופכים את ה-Debug מאמנות למדידה.

State קטן ועקבי עדיף על State ענק מבולגן. אל תשימו בתוכו את כל הלוג הגולמי. שימו ממצאים מתומצתים. אחרת תשלמו על Context בלי תמורה.

שימו ב-State גם את השאלה שהמשתמש שאל בניסוח המקורי. אחרת אחרי Re-plan ארוך תאבדו את המטרה האנושית ותאופטימו למדד הלא נכון.

State הוא גם מוצר: אפשר להציג אותו ל-Operator בזמן ריצה ולהרוויח אמון.

---

## ‏38 — Idempotency, Timeouts, Cost

[סטודנטים · 38]

**על המסך:**

**הגדרה:** Idempotency.

- `idempotency_key` ליצירות  
- Timeouts: Tool / Agent / Task  
- Quality ↔ Cost ↔ Time · סננו Context ל-LLM

**להקראה:**

Idempotency, Timeouts, Cost.

Idempotency: אם create_ticket הצליח אבל התשובה אבדה, Retry בלי מפתח עלול ליצור שני Tickets. לכן idempotency_key.

Timeouts בשלוש שכבות: Tool, Agent run, ומשימה שלמה. בלי זה משהו תקוע יכול לתקוע הכול.

Cost-aware planning: חמש קריאות זולות מול עשרים קריאות ומודל גדול — לפעמים מגיעים לאותה תשובה במחיר שונה לגמרי. מאזנים איכות, זמן וכסף.

וגם Context עולה כסף. אל תשפכו מאתיים אלף שורות לוג למודל. סננו קודם.

ב-Lab שלנו השתמשנו רק ב-read, ולכן הרצה חוזרת לא יצרה משאבים כפולים. ברגע שיש פעולת כתיבה — בלי Idempotency הסיכון קופץ מיד.

חיבור אחרון ל-Cost.

גם קריאת read יכולה להיות יקרה אם אתם שולפים טבלה ענקית לתוך ה-Context. לכן Tool טוב מקבל פילטרים: זמן, רמה, מזהה, Limit.

Planning טוב כולל בחירה של Tool הצר ביותר שמספיק ל-Evidence. זה Cost-aware planning בפועל.

Timeout של Task שלם חשוב במיוחד ב-Agents כי לולאה יכולה להיות חיה וגם תקועה לוגית. Tool timeout לבד לא יציל אתכם מ-Agent שמחליף כלים לנצח בתוך התקציב.

Idempotency לא רק לטיקטים. גם ל-scale up, גם ל-create invoice, גם ל-send email. כל דבר עם תופעת לוואי צריך מפתח או בדיקת מצב לפני פעולה.

Cost-aware planning מתחיל בבחירת Tool צר. זה שיפור מיידי בלי להחליף מודל.

---

# ‏חלק יב׳ — Anti-patterns והדגמות פתרון

## ‏39 — Anti-Patterns

[סטודנטים · 39]

**על המסך:**

1. 40 צעדים במקום 5  
2. Tool success = Goal success  
3. בלי Max iterations  
4. בלי HITL על מסוכן  
5. Prompt ארוך במקום State+Plan  
6. ReAct בלי Evidence gate (חדש)

**להקראה:**

שישה Anti-patterns לסיום החלק העיוני.

אחד: לתכנן ארבעים צעדים כשאפשר חמישה.

שניים: להניח ש-Tool success שווה Goal success.

שלוש: בלי Max iterations.

ארבע: בלי HITL על פעולות מסוכנות.

חמש: Prompt ארוך במקום State ו-Plan אמיתיים.

שש: ReAct בלי Evidence gate — לולאה שרצה גם כשאין מידע חדש.

אם אחד מאלה מופיע בסקירה ארכיטקטונית — עצרו שם. אל תמשיכו לוויכוח איזה מודל יותר חכם. קודם מתקנים את השלד.

איך משתמשים ברשימת ה-Anti-patterns בעבודה.

לוקחים Design Doc של Agent, עוברים פריט־פריט, וכותבים Pass או Fail. כל Fail הופך לטיקט תיקון לפני Production. בלי רחמים על דמו מרשים.

Anti-pattern מספר שש — ReAct בלי Evidence gate — הוא הגרסה המודרנית של while true בלי break. תתייחסו אליו באותה חומרה.

שמרו את ששת ה-Anti-patterns ליד ה-PR template של Agents. אם אחד מהם מופיע — Require change לפני מיזוג.

עברו על ששת האנטי־דפוסים כרשימת בדיקה בקול בסוף כל Design Review עתידי.

---

## ‏40 — הדגמה 1 — תכנון Agent ל-CI (בעיה)

[סטודנטים · 40]

**על המסך:**

מטרה: *״מצא למה Job ב-CI נכשל.״*

Tools: `get_job_status` · `get_console_log` · `get_recent_changes` · `get_runner_info` · `search_previous_failures`

שאלות שנענה עליהן בהדגמה:
1. מה חייבים לדעת קודם?  
2. חובה כל ה-Tools?  
3. אם `get_console_log` נכשל — מה עכשיו?  
4. Stop מול Escalation

**להקראה:**

הדגמה אחת — תכנון Agent ל-CI. קודם מציגים את הבעיה.

המטרה: מצא למה Job ב-CI נכשל.

הכלים הזמינים על המסך: get_job_status, get_console_log, get_recent_changes, get_runner_info, search_previous_failures.

ארבע שאלות שנענה עליהן מיד בהדגמת הפתרון בשקופית הבאה: מה חייבים לדעת קודם, האם חובה להשתמש בכל הכלים, מה עושים אם הלוג לא זמין, ומתי Stop מול Escalate.

זו הדגמת חשיבה בקול של ארכיטקט — נפתור יחד על המסך.

לפני הפתרון: שימו לב שהמטרה היא מצא למה — לא תתקן את הבילד.

אם המטרה הייתה תתקן, היינו צריכים Policy אחרת לגמרי. ניסוח המטרה קובע את גבול הכלים. זו תזכורת קטנה וענקית.

ניסוח המטרה על המסך הוא גם שיעור Product: מצא למה יוצר Agent חוקר. תתקן יוצר Agent מתקן. אל תערבבו בלי Policy מתאימה.

הבעיה על המסך מכוונת במכוון לחקירה. בהדגמה הבאה נפתור אותה בלי לבקש מכם לעצור ולהגיש. המשך ישיר לשקופית הפתרון.

הדגמה 1 מתחילה בבעיה כדי שתראו את הפער לפני הפתרון.

---

## ‏41 — הדגמה 1 — פתרון מלא בקול

[סטודנטים · 41]

**על המסך:**

1. **קודם:** job id · זמן כשל · branch/commit · הודעת כשל קצרה מהסטטוס  
2. **לא חובה כל הכלים** — מתחילים ב-`get_job_status` ואז `get_console_log`  
3. **Log נכשל:** `get_recent_changes` · `search_previous_failures` · אם אין נתונים → Escalate  
4. **Stop:** Evidence ממוקד + המלצה אחת · **Escalate:** אין גישה ללוג/שינויים אחרי 2 ניסיונות  

Plan לדוגמה: status → log → (אם ImportError) recent_changes → confirm dependency → report.

**להקראה:**

הדגמה אחת — פתרון מלא בקול.

קודם כל: לפני כל Tool, Agent חייב לדעת job id, זמן הכשל, branch או commit, והודעת כשל קצרה מהסטטוס. בלי זה החקירה עיוורת.

שנית: לא חובה כל הכלים. מתחילים ב-get_job_status. אם יש כשל ברור — עוברים ל-get_console_log. רק אחר כך מרחיבים.

שלישית: אם get_console_log נכשל — לא נתקעים. מנסים get_recent_changes, search_previous_failures, ואם אחרי שני כיוונים עדיין אין נתונים — Escalate לאדם עם תיאור מה כבר נוסה.

רביעית: Stop כשיש Evidence ממוקד והמלצה אחת ברורה. Escalate כשאין גישה לנתונים קריטיים או כשהתקציב נגמר בלי Confidence סביר.

Plan לדוגמה שמדגימים: status, אחר כך log, אם מופיע ImportError אז recent_changes, אימות dependency, ואז report. זה Closed Loop פשוט עם Budget מובנה.

עוד פירוט על סדר הכלים.

status קודם כי הוא זול ונותן ציר זמן. log אחר כך כי הוא Evidence העשיר ביותר בדרך כלל. recent_changes רק כשיש חשד לשינוי קוד או תצורה. runner_info רק כשיש סימנים סביבתיים. previous_failures כדי לראות אם זה חוזר.

זה לא סדר קדוש — זה סדר עלות־תועלת סביר שמדגימים כאן בקול.

בפתרון המודגם, Escalate הוא לא כישלון אישי של המערכת. הוא תוצאת Stop לגיטימית. צוותים שמענישים Escalate גורמים ל-Agents להמציא תשובות. אל תענישו Escalation תקין.

נעבור על הפתרון לאט כדי לשמוע את סדר קבלת ההחלטות. זה לא קיצור דרך של תשובות קסם — זה המסלול ש-Agent ממושמע אמור לרוץ.

בפתרון המודגם שימו לב לסדר העלות: זול קודם, יקר אחר כך, Escalate כשאין נתונים.

---

## ‏42 — הדגמה 2 — Re-plan מלא

[סטודנטים · 42]

**על המסך:**

הנחה ראשונית: Runner אשם.  
Observation: Runner תקין · חסר dependency ב-`requirements.txt`.

- **Plan מקורי:** runner_info → console → conclude runner  
- **Local re-plan:** אחרי runner תקין → חפש בלוג `ModuleNotFound` / השווה requirements ל-lock  
- **Global:** רק אם מתברר שזה בכלל job אחר / pipeline אחר  
- **Stop:** נמצא החבילה החסרה + שורת לוגו התומכת · המלצה: להוסיף ל-requirements ולהריץ מחדש

**להקראה:**

הדגמה שתיים — Re-plan מלא.

הנחה ראשונית: Runner אשם. זו Hypothesis נפוצה וגם מסוכנת, כי היא מפתה.

Plan מקורי: runner_info, אחר כך console, אחר כך מסקנה שה-Runner אשם.

Observation חדש: Runner תקין, ובלוג חסר dependency בקובץ requirements.

Local re-plan: לא זורקים את כל החקירה. משנים את ההמשך — מחפשים ModuleNotFound, משווים requirements מול lock, וממליצים להוסיף את החבילה.

Global re-plan היה נכנס רק אם מתברר שזה בכלל לא אותו job או לא אותו pipeline.

Stop: נמצאה החבילה החסרה עם שורת לוגו תומכת, וההמלצה האופרטיבית ברורה — לעדכן requirements ולהריץ מחדש.

זו דוגמה קטנה שמכילה כמעט את כל המפגש: Hypothesis, Evidence, Local re-plan, Stop.

למה Wrong assumption על Runner כל כך נפוץ.

כי בעבר זה באמת היה Runner, והמודל או האדם מביאים הטיית זמינות. Evidence חדש חייב להיות חזק מספיק כדי להפיל Hypothesis אהובה. לכן כותבים ב-State לא רק מה מצאנו — אלא גם איזו Hypothesis נפסלה ולמה.

שימו לב שב-Local re-plan שמרנו את מה שכבר למדנו: Runner תקין. לא מוחקים ממצא רק כי ההשערה השתנתה. State צובר. Hypothesis מתחלפת.

הדגמת Re-plan הזו היא גם תבנית שתוכלו להעתיק לטסטים: Given hypothesis runner, When observation requirements missing, Then local replan to dependency. כך בודקים Agents.

הדגמת Re-plan היא תבנית בדיקה. העתיקו אותה לטסט רגרסיה של Agent.

---

## ‏43 — הדגמה 3 — Checklist על Agent לדוגמה

[סטודנטים · 43]

**על המסך:**

עבור CI Agent מההדגמה — סטטוס לדוגמה:

- [x] Goal+Success · [x] Budget · [x] Failure typed  
- [x] Policy read-only · [x] HITL על re-run job  
- [x] State · [ ] Trace מלא (חסר — נוסיף במפגש Observability)  
- [x] Timeouts · [x] Cost limit על tool calls

**להקראה:**

הדגמה שלוש — Checklist על ה-CI Agent שתכננו.

Goal ו-Success: כן. אנחנו יודעים מה נחשב מצאתי סיבה.

Budget: כן. הגבלנו כלים ואיטרציות.

Failure typed: כן. הבחנו בין כשל לוג לבין כשל משימה.

Policy read-only: כן לחקירה.

HITL על re-run job: כן, אם הפעולה משנה משהו בסביבה.

State: כן, לפחות ברמת mמצאים וצעד נוכחי.

Trace מלא: עדיין חסר יחסית — נסמן כפער ונחזור אליו במפגש על Observability.

Timeouts ו-Cost limits: כן.

הערך של Checklist הוא לא הבירוקרטיה. זה לכפות עלינו לראות מה חסר לפני שקוראים למערכת מוכנה ל-Production.

על הפער ב-Trace.

בלי Trace אתם לא יודעים מה Agent עשה אחרי שבוע. בלי זה אין למידה, אין Audit, ואין איך לשפר Prompt או Policy. לכן סימנתי אותו כחסר במודע — לא כדי להתעלם, אלא כדי לא לשקר שמוכנים ל-Production.

כשאני מסמן Trace כחסר, אני מדגים יושרה הנדסית. עדיף חור גלוי על ירוק מזויף. אותו דבר תעשו ב-Checklists בעבודה.

Checklist עם חור גלוי עדיף על מאה אחוז ירוק שקרי. זו תרבות הנדסית.

---

## ‏44 — הדגמה 4 — Design Review מלא

[סטודנטים · 44]

**על המסך:**

תרחיש: *״מצא סיבה ב-Production ותקן.״*  
Tools: `read_logs` · `get_metrics` · `restart_service` · `scale_service` · `deploy_version` · `delete_resource`

תשובות שנקריא:
1. **אוטומטי:** logs + metrics בלבד  
2. **HITL:** restart / scale / deploy  
3. **אסור או תמיד HITL+שני מאשרים:** delete_resource  
4. **דטרמיניסטי בקוד:** allow-list · severity→HITL · max iterations  
5. **Stop:** סיבה סבירה + Confidence בינוני+ בלי תיקון, או אחרי תיקון מאושר + בדיקת מדד  
6. **אנטי-לולאה:** budget · אין retry עיוור על permission denied  
7. **Trace:** כל Tool call + החלטה + אישור אדם בלוג מובנה

**להקראה:**

הדגמה ארבע — Design Review מלא.

התרחיש: מצא סיבה ב-Production ותקן אותה. שימו לב כמה מסוכנת המילה ותקן.

הכלים: read_logs ו-get_metrics הם אבחון. restart_service, scale_service, deploy_version הם שינוי מצב. delete_resource הוא מסוכן מאוד.

תשובות שנקריא במפורש.

אוטומטי: רק logs ו-metrics.

HITL: restart, scale, deploy.

אסור או תמיד HITL עם שני מאשרים: delete_resource.

דטרמיניסטי בקוד: allow-list, מיפוי severity ל-HITL, max iterations, timeouts.

Stop: או סיבה סבירה עם Confidence בינוני ומעלה בלי תיקון, או אחרי תיקון מאושר ובדיקת מדד חוזרת.

אנטי-לולאה: budget, ואין retry עיוור על permission denied.

Trace: כל Tool call, כל החלטה, וכל אישור אדם בלוג מובנה.

זה Design Review אמיתי. לא סיסמה של best practices — אלא החלטות שאפשר ליישם מחר בבוקר.

משפט סיום ל-Design Review.

אם הכלי יכול לשנות Production, ברירת המחדל היא חסום. פתיחה היא החלטה מפורשת עם Owner, לא שכחה ב-Prompt.

זו המדיניות שמונעת את רוב האסונות של Agents עם הרשאות רחבות.

אם הייתי חייב להשאיר רק כלי כתיבה אחד פתוח בלי HITL — לא הייתי משאיר אף אחד מהרשימה הזו. זו התשובה השמרנית, וברוב הארגונים היא הנכונה עד שיש מדידה, Trace וניסיון.

נחזור בקול על delete_resource. גם אם יש HITL, צריך מניעת טעות: הקלדת שם משאב, סביבה, ותקופת חסד. HITL לבד לא מספיק לפעולות בלתי הפיכות.

Design Review נגמר בהחלטות מפורשות על כל Tool כתיבה. אין אפור.

---

## ‏45 — סיכום

[סטודנטים · 45]

**על המסך:**

- Planning = Hypothesis + מגבלות  
- Closed loop + Validation + Budget/Stop  
- Tool ≠ Goal success  
- State · Policy · Idempotency שייכים לארכיטקטורה  
- Lab: ראינו Re-plan על Observation אמיתי/ריק  
- מפגש 3: **Tool Use ו-Function Calling**

**להקראה:**

סיכום המפגש.

Planning הוא Hypothesis תחת מגבלות — לא תסריט קשיח וגם לא קסם.

Closed loop בלי Validation ו-Budget הוא לולאה יקרה.

Tool success אינו Goal success.

State, Policy ו-Idempotency שייכים לארכיטקטורה, לא לנספח עתידי.

ב-Lab ראינו Re-plan על Observation אמיתי או ריק.

המשפט המרכזי: Planning הוא לבחור את הצעד הבא הטוב ביותר תחת מגבלות, ואז ללמוד מהתוצאה.

במפגש הבא נצלול ל-Tool Use ו-Function Calling — המקום שבו ההחלטות האלה פוגשות ממשקי כלים אמיתיים.

אם תזכרו משפט אחד בלבד מהמפגש כולו:

Planning הוא לבחור את הצעד הבא הטוב ביותר תחת מגבלות, ואז ללמוד מהתוצאה.

כל השאר — Decomposition, ReAct, Validation, Budget, Policy — הם פירוט של המשפט הזה.

חזרו למפגש הזה לפני שאתם מתכננים Agent חדש בעבודה. לא כדי להעתיק דוגמאות CI, אלא כדי להעתיק את המשמעת: Hypothesis, Validation, Budget, Policy, State.

סיכום אופרטיבי: לפני מפגש 3, קחו Agent אחד קיים אצלכם ובדקו האם יש לו Hypothesis, Validation, Budget, Policy, State. חמש וי או לא. זה שיעורי בית בלי להגיש.

הסיכום הוא גם מעבר: מפגש 3 ייקח את המשמעת הזו אל תוך עיצוב Tools.

---

## ‏46 — הכנה למפגש 3

[סטודנטים · 46]

**על המסך:**

- 3 Tools מהעבודה: קלט · פלט · מסוכן?/HITL  
- בונוס: סמנו איזה מהם Idempotent

</div>

**להקראה:**

הכנה למפגש 3.

בידקו לעצמכם שלושה Tools מהעבודה: מה הקלט, מה הפלט, והאם מסוכן או דורש HITL.

בונוס: סמנו איזה מהם Idempotent. זה מחבר ישירות למה שדיברנו היום על Retry ופעולות כתיבה.

תודה. נתראה במפגש השלישי.

במפגש 3 ניקח את שלושת ה-Tools שתכינו ונשאל עליהם שאלות קשות: מה התיאור שהמודל רואה, מה ההרשאות, מה קורה בפרמטרים שגויים, ומה ה-idempotency.

ככל שהתשובות שלכם יהיו חדות יותר — כך המפגש הבא יהיה שימושי יותר.

סוף מפגש 2.

רשימת הכלים שתכינו למפגש 3 היא חומר גלם. ככל שתהיו כנים לגבי מה מסוכן — כך נוכל לבנות דוגמאות מדויקות יותר במפגש הבא.

זהו מפגש 2.

הכינו את שלושת ה-Tools בכתב. לא בראש. כתוב. קלט, פלט, סכנה, Idempotent כן או לא. נשתמש בזה במפגש 3 כחומר חי.

סיימנו מפגש 2. תודה. נתראה במפגש 3 עם שלושת ה-Tools שכתבתם.

---

# ‏נכסים

| נתיב | שימוש |
|---|---|
| `assets/02/` | דיאגרמות |
| `assets/screens/` | CI / Metrics / K8s / Approval |
| `labs/02-aws-agent-loop/` | Lab חי read-only |

</div>
